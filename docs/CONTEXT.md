# Current handoff

Updated 2026-09-08. Continue the original non-Stripe goal. Follow AGENTS.md; use one implementation stream and customer acceptance scenarios. GitHub sign-in is complete, so do not ask again. The goal tool still reports an older blocked state; the user has resumed work, and current implementation is progressing. Do not mark complete or declare a new blocker from that stale state.

## What works

- **Hetzner verified:** first internal CLI deployment on cheap CPX12, frontend/backend/PostgreSQL, public HTTPS, disconnect/reconnect, logs, persistent update, exact VM/IP/snapshot/firewall/SSH cleanup. Commits `4d2bffa` + `86b6b1b`; `docs/reference-deployment-verification.json`.
- Customer GitHub device login is verified. Delegation/revocation, SSH/SFTP, durable commands, Compose/recovery, managed/custom routing, encrypted protected backups, daily captures, isolated restore, promotion and route movement are implemented. Later application proofs used native Ubuntu guests, local HTTPS gateways and MinIO, not final customer Hetzner/public ACME/protected Hetzner S3.
- Retention/purge keeps seven newer successful UTC scheduled days; bytes release only after exact-version absence. Uncertain upload recovery reconciles read-only without another PUT. Low scratch capacity refuses transfer without spending attempts and resumes the same operation when space returns. Relevant commits `d40644b` + `340819c`; Linux CI34177467718 and34178283297 passed. Evidence `.local/backup-recovery-smoke-2.log`, `.local/backup-space-smoke-1.log`.
- `acld usage` reports account/grant limits, active rates and retained backup bytes; `usage history` preserves transactional reservation changes with migration-time baselines. Commit `3cc16e2`, Linux CI34179439485 passed. Actual CLI create→power-off→resize→destroy and retained backup→purge→zero passed. Main DB migration0026 is applied; all27 hashes matched `.local/usage-db-check.log`. Applied SQL is immutable.
- Pinned non-root self-host image and loopback simulated Compose support explicit internal bootstrap, packaged CLI, persistent restart and isolated control DB/private identity restore. Snapshots bind hashes, migration state and image; damaged/incomplete input is rejected before target services, and the source remains unchanged. This is a simulated-provider recovery check, not provider reconciliation or continuous WAL recovery.
- Native customer-site Umami proof passed in `12fd9565353c2e67b096bca24f0c4a12954a3cf5`: fresh installed Chrome fetched the actual tracking script, clicked the real frontend button, persisted PostgreSQL count1→2 and produced one pageview/visitor/record-visit event. Both routes and the exact VM were cleaned. `.local/analytics-native-4.log`. Local origin checks include the test gateway port. The browser helper accepts private test certificates only in its fresh restricted context; Node API readback verifies the CA. Public ACME is a separate proof.

## Current checkpoint

Recipe CLI packaging is committed/pushed as `1d43b8f4ce5c208828290b67a63f0a4259b0d3ed`. Installed `acld recipe list/inspect/prepare --version` works offline; authenticated API/SDK share catalog metadata. Pinned assets live in `packages/recipes`; source-only preparation was removed and callers migrated. Actual `.local/recipes-packaged-smoke-2.log` passed private production CLI preparation, PostgreSQL persistence, Umami security/analytics/isolated startup and exact cleanup. Required local check693 tests/67 files passed `.local/recipes-check-final-1.log`. Linux CI34182236233 completed every check, including recipe/self-host restore smokes, but the overall job was cancelled at its15-minute boundary. It is not a green run; current YAML increases the bounded job limit to20 minutes. Do not rerun that unchanged old run.

Current uncommitted WAL checkpoint is verified and ready to commit. `infra/compose/self-host-wal.yaml` adds encrypted pgBackRest2.59.1/PG17.11 on a dedicated `database-wal` volume, separate from Alpine data. Root image pins Bookworm digest051f7b7b3abdd564d5d1bd1e8c4b9c1b6e77087d1dd22020ede611c096a272e0 and pgbackrest2.59.1-1.pgdg12+1 with both architecture hashes. `initialize-wal` creates/reuses private key material. One template at `apps/control/assets/pgbackrest.conf` is packaged into both images. Runtime refuses altered config; restore holds a shared-destination directory lock through pgBackRest and cancellation, and refuses nonempty targets. UTC ISO input is converted with GNU date to PostgreSQL's required recovery_target_time spelling; direct ISO T/Z failed actual startup and was fixed.

Actual combined `.local/selfhost-wal-4.log` passed in81.47s: full backup, later actual CLI project write, saved DB timestamp, another CLI project write, archived WAL, independent key copy, fresh isolated target recovery of the earlier project/exclusion of later project, exact migrations, unchanged source/key/repository, then original control restart/destroy. Missing/wrong/altered keys, competing directory lock and nonempty retry were refused. Target's actual repository mount was read-only with archiving off and no API/worker/host DB port. The same run includes prior dump/private identity restore. `.local/selfhost-wal-cleanup-4.json` confirms all three owned projects, both image tags and temporary private directories absent.

Required `.local/control-wal-check-final-1.log` passed typecheck/lint and696 tests/68 files in180.06s; tool session67044 should be closed if still open. `.local/control-wal-format-final.log` passed. Bounded reviewer confirmed both runtime fixes without residual blockers; root reviewed the fixture's isolation, safe diagnostics and reverse cleanup. Current `smoke:self-host-wal` runs the combined scenario, and CI uses it once instead of the previous subset, with an8-minute step cap. No guest inputs or main DB migrations changed. Final docs only need formatting; commit/push this checkpoint and observe new CI once after useful work.

Earlier failures: `12fd956` CI34181018772 lacked a clean-checkout `.local` snapshot directory; fixed/pushed in1d43b8f using OS tmpdir. WAL smoke1/2/3 restored files but target PostgreSQL rejected ISO recovery_target_time. Both tiny PG probes and all full failed fixtures were cleaned. The initial generic PGDG version was wrong; use the Bookworm-specific pin above. Production pnpm deploy pruned source devDependencies during recipe development; packaging now uses a temporary allowlisted workspace and dependencies are restored. Preserve these structural fixes.

## Next acceptance and remaining work

Next acceptance: exercise loss and independent recovery of the application-backup wrapping-key material against the existing encrypted backup/restore path, then connect guarded provider/control recovery reconciliation. Reuse current keyring and MinIO/native fixtures rather than inventing another backup framework. `scripts/smoke-backup-retention.ts` loads keyring files lazily; `apps/control/src/backup-work.ts` does likewise. Existing native proof covers key rotation, not complete independent key loss. The local pgBackRest key-copy/PITR proof does not establish off-host storage or safe resumption of real-provider mutations after control DB restore. All three subagents are idle; use only bounded independent tasks/reviews.
Remaining original scope includes protected Hetzner S3/IAM, provider automated VM backups, self-host installation with customer runtime/public gateways, independent control/key recovery, provider inventory/revocation reconciliation before restored mutators run, agent/API discoverability and release distribution, appropriate operational failure drills, and final authorized customer Hetzner deploy/update/restore/cutover/revocation/usage/cleanup. Stripe remains excluded. Do not invent new prerequisites or silently omit blocked items.

## Commands and evidence

Repo `/Users/yahorbarkouski/Documents/ChatGPT/learning/agent-cloud`, branch `yahor/agent-cloud`, private origin `yahorbarkouski/agent-cloud`. Use `npm exec --yes --package=pnpm@12.3.4 -- pnpm <command>`. Source checkpoints require `check`, `format:check` and relevant integrations. `smoke:recipes` packages and exercises actual Docker recipes; CI includes it. `smoke:self-host-wal` includes base packaging, dump restore and WAL recovery once; `smoke:self-host-restore` remains a narrower command. Other targeted commands: `smoke:usage`, `smoke:backup-retention`, `smoke:backup-recovery`, `smoke:backup-store`.

Native scenarios run sequentially after `build:guest`: `AGENT_CLOUD_ACCESS_SCENARIO=1 AGENT_CLOUD_HOSTING_SCENARIO=1 AGENT_CLOUD_ANALYTICS_SCENARIO=1 node node_modules/tsx/dist/cli.mjs scripts/smoke-guest.ts`; backup mode uses `AGENT_CLOUD_BACKUP_SCENARIO=1`, optional schedule mode `AGENT_CLOUD_BACKUP_SCHEDULE_SCENARIO=1`. Selected image `1093b9f46fef0b9015c987f11f3d30e3aa2eeea168a597e02acc7eec908aa27b`; no guest source/published assets edited in this checkpoint. Orb syntax has no `--` separator. Failures retain an exact ownership receipt; inspect/delete only the matching fixture before a new smoke.

## Active resources and blockers

No paid resources created this checkpoint. WAL source `acld-self-host-0eaf524c-4085-4c7d-b65a-1c3c254e7097`, dump target `acld-self-host-restore-8e7f5976-bf01-4af0-b1cf-90b6aa5a4800` and WAL target `acld-self-host-wal-d0a5034d-cc5d-4bc6-8ed2-4af12a69f480` are cleaned. Untagged image IDs in `.local/control-postgres-development.image` may already have been removed with identical fixture tags; do not assume those stale local receipts identify a running resource. Recipe projects `acld-recipe-31d1db0a-5f1d-4744-9d45-2f19d92cef35` and `acld-recipe-94becf68-cafb-4686-908e-09e0e85a0846` were cleaned. No native VM/receipt remains. Self-host fixtures and image tags/private snapshots were cleaned; shared build caches remain. Dev Hetzner project15945891 last verified empty after the internal live proof; default project untouched. No Hetzner bucket exists.

Preserve local PG `agent-cloud-dev-postgres-1` localhost55439, CA `agent-cloud-pki-ca-1` https://localhost:9449, `.local/pki`, `.local/runtime-identity`. Main API PID15176/worker15190 have older loaded code. GitHub app Agent Cloud Development ID3843400, public clientID `Ov23liZVS2XqBNSrJQsi`; `.local/github-oauth.json` and `.local/hcloud-token` are0600.

Caps: image VM/IP120000µUSD, customer VM/IP60000µUSD, snapshot monthly1000000µUSD. Prior internal VM/IP estimate83394µUSD excludes storage and is not an invoice. No S3 cap yet. Public Object Storage pricing on2026-09-08 showed €6.49/month excluding VAT; its hourly field rendered blank. Obtain the current hourly quote and record a cheap cap before paid storage. Runtime S3 credentials must be in a separate Hetzner project from the bucket/admin, bound by an exact cross-project principal policy; separate files alone are not least privilege.

Pending external action: Hetzner browser tab5 is signed out. One async login question is pending for scoped S3 credential creation; VM API token still works. Do not repeat that question. CUA also reported the Mac locked and a single unlock question was sent. Native analytics now uses local headless Chrome, so the lock is no blocker for that proof. Browser MCP later disconnected; do not repeatedly retry it. GitHub login is already done.

Earlier full process output exposed ambient Anthropic, Braintrust and Apple app credentials; user was told to rotate, status unverified. Never reproduce values or full process arguments/environment; use PID/command-name diagnostics only.
