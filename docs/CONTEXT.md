# Current handoff

Updated 2026-09-08. Continue the original non-Stripe product goal on `yahor/agent-cloud`. Follow AGENTS.md and working customer acceptance scenarios. GitHub sign-in is complete; do not ask again. The goal tool's old blocked state is stale, and implementation is progressing.

## What works

- **Hetzner live:** internal CLI deployment on cheap CPX12, frontend/backend/PostgreSQL, public HTTPS, disconnect/reconnect, logs, persistent update and exact VM/IP/snapshot/firewall/SSH cleanup. Commits `4d2bffa` + `86b6b1b`; `docs/reference-deployment-verification.json`.
- **Customer access:** actual GitHub device login. Delegation/revocation, SSH/SFTP, durable commands, Compose updates/recovery and managed/custom routes are implemented. Native Ubuntu checks verified revocation/API-outage closure, guest restart and persisted application updates. Later proofs use local gateways, not final customer Hetzner/public ACME.
- **Application data:** encrypted exact-version Object Lock backups, daily capture, retention/purge, uncertain-upload recovery, low-scratch pause/resume, isolated PostgreSQL/file restore, promotion and HTTPS route movement. Actual local MinIO/native proof includes count1 restored and count2 written after cutover. No Hetzner bucket has been provisioned.
- **Recipes/analytics:** production CLI bundles versioned PostgreSQL/Umami recipes and works offline outside the checkout. Actual Docker persistence/security/isolation verified. Native fresh Chrome clicked the reference frontend and produced a pageview/visitor/event. Commits `1d43b8f` and `12fd956`; `.local/recipes-packaged-smoke-2.log`, `.local/analytics-native-4.log`.
- **Usage:** CLI/API limits, active VM rates, retained bytes and append-only reservation history. Actual create→power-off→resize→destroy and purge→zero verified. Commit `3cc16e2`; migration0026 applied to main DB and all27 migration hashes match `.local/usage-db-check.log`.
- **Self-host recovery:** pinned non-root runtime, persistent loopback simulated installation, isolated dump/private-identity restore and encrypted pgBackRest WAL/PITR. `9d21763cfd38ecbc73ca7db9176cc3a24809e5f7` passed Linux CI34184253818. Native Docker proof `.local/selfhost-wal-4.log` restored a selected earlier CLI project, excluded a later write, preserved source/key/repository and checked missing/wrong keys, competing lock, nonempty-target refusal and exact cleanup. This does not prove off-host storage or real-provider fencing.

## Current capability checkpoint

`6a74f2e54d010a0828b8a0b97e59fce0f0ded89c` commits fenced control recovery. API/worker/operator mutations require a ready external generation and live shared database lease. Recovery revokes stale authority and schedules, checks exact provider inventory and retained image/backup receipts, closes externally settled attempts through immutable receipts and resumes only with matching state/inventory digests. It performs no provider mutations. See `docs/control-recovery.md` for supported states and external fencing requirements.

Recovery proof: `.local/control-recovery-check-4.log` required checks743/74 and format passed. `-dump-smoke-2.log` passed real pg_dump/pg_restore with post-checkpoint source revocation, restored API/worker refusal, operator begin/resume, stale token401 and preserved application records. `-packaged-1.log` passed combined packaged CLI/API/HTTPS gateway/dump/WAL checks. `-tests-6.log`, `-uncertain-attempt-1.log` and backup retention/repair logs verify delayed creates, forged-receipt refusal, lease loss, read-only image/backup evidence and exact cleanup. Linux CI34192771022 passed. Prior gateway commit `a902bad` passed CI34189304610.

`44f16621f0b9d05589128fbf64ab1359ea25d1ad` commits the standalone CLI archive with production dependencies, original licenses, recipes and offline agent instructions. Required `.local/cli-release-check-2.log` passed751 tests/75files and format passed. `.local/cli-release-smoke-2.log` verified archive corruption refusal, forced stale-output replacement, extraction outside checkout, symlink launcher, private skill installation, recipes, HTTP login/commands and revocation. Retained clean-commit artifact `.local/releases/cli-0.1.0-44f1662/agent-cloud-cli-0.1.0.tar.gz` is2601489bytes, SHA256`9ecddc182b515a28cb87f9dcc0ac5f6e5669c17395cc674181a0a6f942f6baea`. Checksums are not publisher signatures. No public artifact uploaded.

CI34194474534 passed CLI release checks but failed later when the existing SSH fixture scanned a just-opened TCP port before sshd was ready. Current code waits for an SSH banner. `.local/selfhost-customer-ssh-readiness-1.log` verifies retry after closed/HTTP sockets and split banner; `-ssh-smoke-1.log` passed both actual allocation/image-verifier SSH proofs and trust refusals.

`a1a08fc`, pushed with the customer checkpoint, adds `/openapi.json` and `/llms.txt` for actual customer/auth routes. Factory mode exposes neither; optional operations follow configured services. Five actual HTTP tests pass. Discovery helper's isolated work is merged; its worktree `.local/worktrees/http-discovery-mtsa70fb` is retained but inactive.

`da9af343aa45c18482898e4292864fbf344c9be2`, committed and pushed, packages customer admission/disable/renew in the runtime and adds `infra/compose/customer.yaml` with private configuration, separate gateway credentials and bounded resources. The public gateway serves a reserved control hostname and optional exact `/v1/ssh` path alongside applications. Restart applies current operator ingress settings to retained app routes. Application publication/domain claims cannot take the control hostname. See `docs/self-host-customer.md`.

Focused evidence: `.local/selfhost-customer-typecheck-3.log` passed; `-tests-1.log`52 tests across gateway/hosting/login and `-tests-2.log`6 operator/discovery tests passed; `-compose-check-1.log` verifies resolved private ports, mounts, networks and tmpfs. Bounded review found and fixed an incorrectly split tmpfs option and root-owned scratch volume. Packaged fixture now exercises operator admission, real customer auth routes with a fixture-only GitHub verifier, actual CLI over trusted HTTPS, disable/revocation, gateway restart and scratch ownership/write. The running fixture receives only the public CA, never gateway private state. `.local/selfhost-customer-packaged-2.log` passed the combined actual container run: customer operator/HTTPS CLI/revocation, gateway mTLS/Host isolation/restart/outage, dump restore and encrypted WAL/PITR. Exact fixture containers/networks/volumes/images were removed. First run rejected a fixture control field at the wrong JSON level; the corrected configuration passed.

The initial full check had759 passing tests and only3 recipe refusal timeouts. Their script packaged the CLI before checking ownership. It now checks collisions before packaging and retains the check immediately before mutation; subprocess timeout is shorter than its test timeout. `-recipe-refusal-1.log` passed3/3 in1.43s. `-recipes-1.log` passed actual packaged PostgreSQL persistence and Umami visitor/pageview/event/isolated startup, with exact cleanup. Three interrupted directories were individually checked for ownership/open files and removed. Required `-check-2.log` passed762 tests/78files, typecheck/lint/build; `-format-2.log` passed.

## Next acceptance and remaining scope

Customer checkpoint is pushed; Linux CI34197235238 is pending. The next source push adds release configuration and supersedes that run through existing safe concurrency cancellation. Do not rerun passed packaged gateway/recovery or recipe integrations without a new concern. Next is verifiable signed CLI distribution and public source. A bounded reviewer classified all16 redacted gitleaks history findings as synthetic fixture values, public hashes/UUIDs, commit IDs or source/prose. Exact fingerprints are in `.gitleaksignore`; `.local/source-history-reviewed.log` reports79 commits scanned and no remaining findings. A separate in-memory comparison of1707 tracked blobs found neither active Hetzner nor GitHub OAuth credentials. No visibility change or release yet. The scanner is checksum-verified gitleaks8.30.1 under `.local/tools/gitleaks-8.30.1`. GitHub OIDC attestations require public repositories on ordinary plans. Current uncommitted `.github/workflows/release-cli.yml` builds in a read-only job, transfers the archive through GitHub artifacts, then signs/verifies/publishes from a separate job without dependency or archive execution. It requires successful named source and archive checks on the exact source SHA; formatting-only jobs cannot qualify. Actionlint1.7.12 and the actual gate predicate pass. The bounded review found only that formatting-only gate issue, now fixed. SECURITY.md, CONTRIBUTING.md and installation signature instructions are prepared. Next: commit/push, finish CI, make the audited source public, enable private vulnerability reporting, tag a checked developer preview and verify its downloaded attested artifact. Do not claim published/signature verification before actual proof.

Remaining original scope: protected Hetzner S3/IAM; live provider automated backups; full self-host customer/public ACME proof; independent provider/signing/bootstrap credential recovery; signed/public distribution; operational failure drills; final authorized customer Hetzner deploy/update/restore/cutover/revocation/usage/cleanup. Pending SQL/route/image recovery may need dedicated reconciliation. A new generation cannot fence processes using an independent old database; external credential/process fencing and delayed-request accounting remain required. Stripe is excluded; simulated/internal access stays private.

## Resume commands and durable lessons

Repo `/Users/yahorbarkouski/Documents/ChatGPT/learning/agent-cloud`, private origin `yahorbarkouski/agent-cloud`. Use `npm exec --yes --package=pnpm@12.3.4 -- pnpm <command>`. Source checkpoints require `check`, `format:check` and relevant integrations. Native backup: after `build:guest`, `AGENT_CLOUD_ACCESS_SCENARIO=1 AGENT_CLOUD_BACKUP_SCENARIO=1 node node_modules/tsx/dist/cli.mjs scripts/smoke-guest.ts`. Native VM smokes run sequentially. `smoke:self-host-wal` includes base packaging, dump restore and WAL once; `smoke:recipes` exercises packaged CLI/Docker recipes.

Package production output in a temporary allowlisted workspace: pnpm12 deploy can prune root devDependencies. Fixtures must use OS tmpdir when no ignored `.local` exists. Applied migrations and published images are immutable. Keep docs in their subsystem files, historical evidence in git/archive, and this as the only current handoff. CLI and self-host bounded reviews are complete. All helpers are idle; bounded source/release reviews are complete. Root owns the primary stream; do not share generated build outputs across worktrees.

## Resources, caps and blockers

No active task-owned native VM or backup store remains. Source `agent-cloud-image-32107b88` and restore `agent-cloud-backup-3ea58b36-0583-48b9-ae88-120c53e6c408` were removed and absence independently verified. Historical backup-target receipts marked deleted are not active resources. No paid resources were created. Read-only `.local/hetzner-customer-read-check-2026-09-08.json` at06:53:23Z authoritatively returned zero servers, IPs, snapshots, firewalls and SSH keys. Control-recovery isolated databases/files were cleaned after tests, dump smoke and the uncertain-attempt smoke. Migration0027 is not applied to the preserved main DB. WAL/recipe fixtures and image tags were cleaned; shared caches remain.

Preserve local PG `agent-cloud-dev-postgres-1` localhost55439, CA `agent-cloud-pki-ca-1` https://localhost:9449, `.local/pki`, `.local/runtime-identity`. Main API15176/worker15190 have older loaded code. Live published guest input remains1093b9f46fef0b9015c987f11f3d30e3aa2eeea168a597e02acc7eec908aa27b. This native run built bde4feb6ec154416502ac8269a25980af2f0b499ef31656856fbd19ef858f012 without modifying the prior artifact.

Hetzner dev project15945891, `.local/hcloud-token`0600; default project untouched. GitHub app Agent Cloud Development ID3843400, public clientID`Ov23liZVS2XqBNSrJQsi`, `.local/github-oauth.json`0600. Caps: image VM/IP120000µUSD, customer VM/IP60000µUSD, snapshot monthly1000000µUSD. Prior internal VM/IP estimate83394µUSD excludes storage and is not an invoice. No S3 cap yet; get an actual current hourly quote before paid storage. September8 public page showed€6.49/month excludingVAT but blank hourly field. Runtime S3 keys must be in a separate project from bucket/admin with exact cross-project principal policy.

External blockers: Hetzner browser session expired, and CUA reconfirmed the Mac locked this turn. One login and one unlock question are already pending; do not repeat. VM API token works. Browser MCP disconnected; do not repeatedly retry. GitHub is already logged in. Earlier full process output exposed ambient Anthropic/Braintrust/Apple credentials; user was told to rotate, status unverified. Never reproduce values or full process arguments/environment; use PID/command-name diagnostics only.
