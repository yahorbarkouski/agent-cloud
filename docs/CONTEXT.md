# Current handoff context

Updated 2026-09-07. Full cloud goal remains active, excluding Stripe. M0 is complete; M1 is close to its customer drill, and M2–M7 remain open. Work only in this standalone repository. Customers bring existing agents. Implementation, private pushes and bounded cheap Hetzner tests are authorized. No expensive plans, benchmarks, warm pool or automatic type/region fallback.

## Current checkpoint

Branch `yahor/agent-cloud`, private origin https://github.com/yahorbarkouski/agent-cloud.git. HEAD2276c43a14511b6c09e0d97e43048abe959497e9 corrects customer firewall22/8443, pushed and CI34131266130 passed full checks, formatting, cleanup and fresh native smokes. Parser6ad8e7ef577f92bb951fb9cf12a92ecca230f9e5 already passed CI34130515183. Prior55b037761e5215c576998455dcca8edda6f48adf implements operator recovery. It is pushed and exact Linux CI34127373737 passed full checks, formatting, CLI cleanup, fresh PKI, SSH and enrollment smokes, and disposal. `.local/operator-recovery-ci-result.json`. The prior customer cleanup implementation950ba8c137dc5a44e36e44646ee62dd0a4251536 also passed exact Linux CI34124600353. Documentation now records both results.

`pnpm machine:recover inspect <cleanup-operation-id>` returns retained scope, attempts, resources, recoveries and a state digest. `apply <private-request.json>` records either explicit `close_create` evidence or one additional exact-target `retry_delete`. Requests require pre-existing customer destroy authority. They serialize under the machine lock, bind the inspected state, and atomically save authority, audit and worker wake-up. Original provider outcomes remain immutable. Closure uses the distinct `operator_closed` resolution, never fake success or absence. New inventory discoveries persist even when admission rejects the stale digest. Every historical source resource ID must be acknowledged. Retry allowances use one SQL function shared by operator admission and the worker. Exact labels, IP assignment, absence, existing backoff and terminality remain enforced.

`HetznerInventory` is the extracted read-only transport. The operator command needs no images, prices, bootstrap or signing configuration. No public HTTP route or customer privilege was added. Provider closure is an explicit attestation backed by operator-maintained evidence. Software validates its scope and provenance, not the truth of external provider confirmation. Never manufacture that evidence from empty inventory or timeouts. See `docs/architecture/operator-recovery.md`.

Full94746 passed420tests/36files, typecheck/lint,104.25s. `.local/operator-recovery-check-2.log`. Independent gpt-5.6-sol review reran33cleanup tests successfully and found no production blocker. Native cleanup60304 passed actual CLI/HTTP/PostgreSQL/Graphile cancellation with delayed simulated inventory and complete disposal. `.local/operator-recovery-cleanup-smoke.log`. Restarted API/worker CLI49678 passed create/inspect/destroy and disposal. `.local/operator-recovery-local-smoke.log`. Customer skill validation passed. Evidence preserves intermediate fixture, type and lint failures in `docs/research/m1-operator-recovery-verification.json`. No active transcript directory was supplied; the reviewer used artifacts and the decision trail.

Migration0019 applied to development after stopping exact owned PIDs38043/38044. All20 hashes match. New hash92dcf490e4a775ed21daa5a69beefc67e914d308e0f55b87201722c8562f931b is immutable. All0000–0019 are applied: do not edit them; future changes need0020. `.local/operator-recovery-migration-check.json`. New services are API42350/worker86187 on4319. SQL at13:22:12Z shows zero active allocations, simulated VMs/IPs and unfinished image builds. Actual Hetzner inventory in `.local/operator-recovery-provider-inventory.json` has zero servers, Primary IPs, snapshots, firewalls and SSH keys. No paid mutation or native VM occurred in this slice.

## Active customer drill

Build653dd67f-c002-4796-b894-27865aed9ec2 reached verified retained publication at13:43Z. Snapshot429179158 remains billable and retained for the customer drill. Builder164967515/IP148476735, verifier164967728/IP148477001, build firewall11587209 and SSH key118468670 are all absent; local builder access is removed. Image watcher47872 exited0. `.local/customer-drill-image-watch.json`, `.local/customer-drill-build.latest.json`, `.local/customer-drill-release.json`. Separate customer firewall11587191 still exists and needs exact deletion after customer cleanup.

The first customer create at13:48Z exposed the actual Hetzner `assignee_type: server, assignee_id: null` response. The strict parser rejected it, leaving IP148478037 as an unknown create and blocking inventory. No VM was created. The lifecycle driver80308 and customer worker51174 were stopped; actual CLI destroy admitted cleanup on the same operationop_e080a041-7140-470a-aa29-41f9bb7ddb79. After the parser correction and9passing transport tests/build, the restarted worker reconciled that original IP and deleted it. At13:57:35Z, exact IP absence, operationcancelled, machinedestroyed, zero live allocation/resources and one original IP create plus one delete were verified. `.local/customer-drill-primary-ip-wire.json`, `.local/customer-drill-first-cleanup.json`, `.local/customer-drill-lifecycle.json`. First attempt archived as `.local/customer-drill-lifecycle.first.json` and `.first.log`. Fullcheck2086 passed422tests/36files, typecheck/lint in100.17s; independent gpt-5.6-sol review found no blocker. Formatting passed; parser6ad8e7ef577f92bb951fb9cf12a92ecca230f9e5 is pushed and CI34130515183 passed including fresh native smokes and cleanup.

Preflight source inspection found a second mismatch before renting a customer VM: guest Caddy listens on8443 with mutual TLS but the operator firewall allowed22/80/443. The correction admits exactly22/8443 and rejects missing8443, unused web ports, duplicates and outbound restrictions. Updated the exact owned unattached firewall11587191 after saving intent; read-back verification passed. `.local/customer-drill-firewall-correction.json`. Private lifecycle driver now probes8443 and uses fresh namecustomer-drill-2224a90a-2. Fullcheck47080 passed423tests/36files, typecheck/lint in104.68s; independent gpt-5.6-sol review found no blocker. CustomerAPI23109/worker4436 restarted with this correction; full dependency/public preflight passed. Second lifecycle driver19160 exited2 after its test assertion failed and cleanup completed. It started14:14:00Z. Machinevm_c74df458-e043-48e2-ac50-de74afeecdcc, allocationalloc_351b4ead-56cc-4be9-8612-28c8592565b1, server164971891/IP148481208 passed real create/enrollment/runtime, power-off/on and reboot14:16:44Z. The script's SSH certificate parser incorrectly stripped a hostname column that `ssh-keyscan -c` does not emit. It failed before the renewal wait and automatically destroyed the VM/IP by14:18Z. Exact provider absence was independently confirmed14:21Z. `.local/customer-drill-lifecycle.second.json`/`.second.log`, `.local/customer-drill-second-provider-state.json`. The raw old scan was not saved, so do not claim it matched the issued certificate. Guest lifecycle proof remains valid; real renewal remains unverified.

New committed-script support `scripts/support/ssh-keyscan.ts` compares the actual certificate fields, rejects missing/plain/multiple/hostname-prefixed output, and is used by native `smoke-ssh.ts` and the private driver. Native9836 first failed because the fixture runner returns `{stdout,stderr}`, not a string; that and static/lint errors were corrected. Native/static26035 passed both allocation and image-verifier subjects with disposal. `.local/customer-keyscan-native-final.log`, `.local/customer-keyscan-static-final.log`. Independent review is running. The private driver now saves public TLS/SSH observations before assertion and uses fresh namecustomer-drill-2224a90a-3. It has not started. `.local/customer-drill-third-budget.json` conservatively totals56826µUSD for the first IP and two one-hour-rounded customer VM/IP runs, below the60000µUSDcap. Source code remains2276c43 plus this test/helper change; no guest input change.

Only snapshot429179158 and free customer firewall11587191 remain now. Keep their cleanup owned.

Current input manifest739d94533985de55963de5486a656ce8ceff67cdd3b2ab7f0e593177f31bc010, versiondev-aec4ae5feb7aefb4e58e4519, input digestaec4ae5feb7aefb4e58e4519ea98c89e8b96d5058cc3e49058f6ac08584a349c, transfere26ea44aebba2595beab5419327cf15e1d10bf1a998aad68afccac18d9b371be. Shared contract changes entered the guest bundle before this build. Published trees are immutable. The provider-only parser correction does not change guest inputs.

Free tunnel processes52514 image and40496 customer expose https://adequate-rarely-turbo-house.trycloudflare.com and https://crossword-tracks-softball-exception.trycloudflare.com. Image API60800/worker66192 use4320. Customer API23109/worker4436 use4321 with the parser and firewall corrections; older customer processes stopped. Main simulated services remain42350/86187 on4319. Live processes use ignored `.local/customer-drill-{image,customer}.env` and the dedicated databaseagentcloud_live_2224a90aacad42f49081e8fc08822ff7. Both public route checks and full customer dependency preflight passed. The dedicated USD credential is `.local/customer-drill/.local/admin.credentials.json`, never the default EUR profile.

Live ownership/config is `.local/customer-drill-record.json`; its private database URL must not enter tracked documents. Input/prepared/admitted/started build records and logs share the customer-drill prefix. Original runtime identity and CA remain unchanged. Authorized caps are$0.12 image VM/IP,$0.06 customer VM/IP,$1 monthly snapshot storage, oneCPX12nbg1 customer VM and50minutes. No artificial issuance-clock changes. Retained snapshot expiry is around16:35Z, verify exact admission before reusing. After the fresh customer drill, finish snapshot cleanup and exact firewall deletion, independently inspect project inventory, stop only recorded temporary processes and drop only the owned live database after absence proof.

## Existing behavior and proof

Customer destroy cancels an active create within its original operation and ends `cancelled` after cleanup. A failed or ready terminal create retains its result while a new destroy handles cleanup. Immutable allocation-scoped authority survives grant revocation for owned deletion only. Unknown creates never retry. Duplicate IDs, original receipts and image pins survive uncertainty. VM deletion cannot implicitly auto-delete an IP while a source VM create remains pending. Customer cleanup passed407tests plus native CLI cleanup before this extension; exact CI34124600353 passed. See `docs/architecture/machine-cleanup.md` and its verification/review artifacts.

Explicit customer runtime157da958d54d87e5c15e0a6c7c8ceac01f5fe636 passed383tests and exact CI34119114439. It connects pinned signed renewal-capable images, encrypted bootstrap, enrollment, renewal, readiness and customer lifecycle jobs. Startup and exact-resource cleanup work without private guest material, current default-image availability or pricing. Fresh work checks its own dependencies. Customer power-off uses graceful shutdown and observed off. No customer Hetzner boot is yet claimed.

Renewal7843b036da6f9d3bf968366576549912f183e6af passed exact CI34115440102 and local native VM/service checks. Original P-256 signatures, database-time freshness, current owned VM/IP, pinned SSH proof, four-per-hour persisted signing attempts, immutable identity history and atomic certificate generations are implemented. Enrollment clock follow-upb16466b9624f98b889f5a41e0a289d4507c5e2a6 passed CI34116511248. See the renewal and customer-runtime architecture/evidence documents.

Corrected real Hetzner image buildde7d3f33-0f3e-4512-afc6-d163b1cd7359 passed installation, sanitation, graceful shutdown, snapshot boot, verifier enrollment/runtime and signed publication/selection. All seven owned resources and local keys were removed10:35:25Z; selection now correctly rejects its deleted snapshot. `docs/research/m1-hetzner-durability-drill.json` and `m1-hetzner-durability-release.json` retain evidence. The earlier failed buildbbb28cd4-aca1-4a94-87cf-364728bf6771 exposed empty installed files after hard poweroff; preserve that failure separately. The correction flushes the filesystem and stops the random-seed writer before sanitation. Native OrbStack virtualization overrides belong only to fixtures, never published inputs.

## Images, services and secrets

Commands use `npm exec --yes --package=pnpm@12.3.4 -- pnpm <command>`. Pnpm uses Node24.10.0; system Node26.5. TS scripts importing src/\*.js need tsx. PostgreSQL17 is127.0.0.1:55439, database/useragentcloud with passwordlocal-development-only. Smallstep CA https://localhost:9449 uses256MiB/.5CPU and pinned step0.30.6/step-ca0.30.2. `.env` remains simulated. Current API42350/worker86187 logs are `.local/simulated-api.log` and `.local/simulated-worker.log`.

Provider projectagent-cloud-development15945891; Default untouched. `.local/hcloud-token`, `.local/admin.credentials.json` and identity/PKI secrets are0600. Never print them or put tokens in argv/guest data. `.local/runtime.json` remains image_factory. Preserve `.local/runtime-identity` private material. Public release key ID6c4eb920bf75d1b753253c327082b3c1386136928830e50779b9efc2e1c1a481. Policy reloads for every authorization; restart API and worker after private changes. Temporary live processes are listed above. No native OrbStack VM is running. Do not reuse a stopped tunnel URL.

## Next work

Finish keyscan review/format/push/CI, then start the third bounded customer driver. Firewall2276c43 exactCI passed; preflight passed. Real elapsed timer renewal and full test resource cleanup remain required. Reuse the retained verified snapshot; do not build another image for a provider-only parser fix. Keep ownership of snapshot/firewall cleanup throughout. Both earlier customer attempts are fully cleaned. No customer VM or IP is active.

Continue M2–M7 customer access/device login, persistent commands/file transfer, Compose deployment, HTTPS/routes, recipes/backups/restore, usage and self-hosting. Image and lifecycle proof do not complete the application product. PROGRESS and DECISIONS retain earlier checkpoints; this file keeps only current execution context.

M2 architecture grounding is in progress while M1 runs. Auth and SSH explorer artifacts are `.local/m2-grounding-auth.md` and `.local/m2-grounding-ssh.md`; `docs/research/m2-customer-access-grounding.md` now contains the synthesis. Three independent SSH design candidates are complete under `.local/m2-access-design/candidate-{a,b,c}`. Judge `.local/m2-access-design/judge.md` recommends A21/25 over C18/B15; parent read all candidates and agrees on A with corrections. Shared task/rubric are in that directory. Read all candidates, request cross-model judgment only after they finish, then select/graft before implementation. Protocol notes are in `docs/research/m2-access-protocol-notes.md`. No M2 source implementation or migration change has started.
