# Current handoff

Updated2026-09-08. Continue the original non-Stripe product goal on `yahor/agent-cloud`. Use working customer acceptance scenarios and AGENTS.md. We build the cloud for existing agents. GitHub sign-in is complete; do not ask again. The goal tool's old blocked state is stale.

## What works

- **Hetzner live:** the first internal CLI/API deployment passed on cheap CPX12: frontend/backend/PostgreSQL, public HTTPS, disconnect/reconnect, logs, persistent update and exact VM/IP/snapshot/firewall/SSH cleanup. `4d2bffa` + `86b6b1b`; `docs/reference-deployment-verification.json`.
- **Customer operations:** real GitHub device login, delegated access/revocation, SSH/SFTP, durable commands, Compose deployment/recovery, limits/usage and managed/custom routing. Native Ubuntu proofs include access closure after revocation/API outage, restarts and persisted application updates. Final public customer Hetzner/ACME proof remains.
- **Data and recipes:** encrypted exact-version Object Lock backups, daily capture, retention/purge, uncertain upload repair, low-scratch pause/resume, isolated PostgreSQL/file restore and HTTPS cutover. Native MinIO proof restored count1 and persisted count2 after promotion. PostgreSQL/Umami recipes are packaged; actual browser/Docker proofs include persistence, visitor/pageview/event, private credentials and restore isolation. Hetzner S3 enforcement is unverified.
- **Control recovery:** `6a74f2e` passed CI34192771022. External control generations and live leases fence restored API/worker/operator mutations. Recovery revokes stale authority, checks ownership/image/backup receipts and requires explicit closure of delayed effects. Actual dump/WAL, lease-loss and uncertain-operation proofs passed. See `docs/control-recovery.md` for unsupported pending states and external fencing requirements.
- **Customer self-hosting:** `da9af343aa45c18482898e4292864fbf344c9be2` packages customer admit/disable/renew and `infra/compose/customer.yaml`. Public gateway serves control HTTPS and optional exact SSH upgrade path beside app HTTPS; apps cannot claim the control hostname. Private credentials are separated. `docs/self-host-customer.md` owns configuration.
- **Agent distribution:** `44f1662` built a standalone portable CLI archive with original licenses, dependencies, recipes and offline skill installation. `a1a08fc` exposes only configured customer/auth operations through `/openapi.json` and `/llms.txt`. `ce9a0d8264ab3b10352fe82860e150fd8e75832c` adds the signed preview workflow and contribution/security guides.

## Current acceptance and evidence

The self-host customer capability is locally verified and committed. `.local/selfhost-customer-check-2.log` passed762 tests/78files plus typecheck/lint/build; `-format-2.log` passed. `-packaged-2.log` passed actual packaged operator admission, CLI over trusted HTTPS, disable/revocation, gateway mTLS/Host isolation/restart/outage, dump restore and encrypted WAL/PITR. It verified non-root scratch ownership/write and exact fixture cleanup. `-recipes-1.log` passed actual PostgreSQL/Umami Docker behavior and cleanup. Public ACME/provider storage are separate.

Concrete fixes: the review caught a split tmpfs option and root-owned scratch; the container fixture caught a control field at the wrong JSON level. SSH fixtures now wait for a real banner before certificate scanning, fixing CI34194474534's startup race; `-ssh-smoke-1.log` passed both native trust proofs. Recipe refusal fixtures now check ownership before expensive packaging, recheck before mutation and terminate children before their test timeout. The previous full check had759 passes and only3 such timeouts; focused refusal now takes1.43s. Three interrupted directories were ownership/open-file checked and removed.

The source repository is now **public** at `yahorbarkouski/agent-cloud`; main was fast-forwarded to `ce9a0d8`. Anonymous LICENSE download matches source. GitHub private vulnerability reporting is enabled. Source history scan with checksum-verified gitleaks8.30.1 found16 false positives, individually reviewed as synthetic fixture values, public hashes/UUIDs, commit IDs or source/prose. `.gitleaksignore` excludes exact fingerprints only. `.local/source-history-reviewed.log` reports79 commits and no remaining findings. In-memory comparison of1707 tracked blobs found neither active Hetzner nor GitHub OAuth credentials.

**Signed public preview is verified.** Linux CI34198076587 passed on exact `ce9a0d8264ab3b10352fe82860e150fd8e75832c`. Release CLI34199568712 then built and published `cli-v0.1.0` at that same revision. [Public release](https://github.com/yahorbarkouski/agent-cloud/releases/tag/cli-v0.1.0), published2026-09-08T07:30:12Z. Obsolete CI34197235238 and duplicate main CI34198925036 were cancelled safely; no equivalent source suite was rerun for the tag.

`.local/public-cli-download-verification-1.log` proves anonymous download, all three file attestations against the exact repository/workflow/ref/SHA, tampered-archive and wrong-ref refusal, extraction outside the checkout, executable version, offline skill installation and both recipe preparations. No credentials were read/created and temporary files were removed. Retained public artifact `.local/releases/cli-0.1.0-github-ce9a0d8/agent-cloud-cli-0.1.0.tar.gz` is1355571bytes, SHA256`9b47030be2db94369f691891bedd69c6f274a8f5a497a853931ee72a6fd3c4da`. The same directory holds release.json, SHA256SUMS and provenance.jsonl.

Release build has read-only permissions; its separate signer/publisher installs or executes no dependency/archive code. Its gate requires the exact-SHA successful Check job plus successful named `Validate source` and `Exercise CLI archive` steps. A bounded review caught formatting-only CI qualifying; the corrected actual jq predicate rejects docs-only, failed-archive and missing-marker results. Actionlint and formatting passed; actual GitHub publication and independent verification now passed too. Published assets and tags must never be overwritten. Future releases require an explicit version change and fresh source evidence.

**Next acceptance:** once the pending console unlock/login is available, obtain the exact Hetzner S3 quote, record a low cap and create separate bucket/admin and scoped runtime credentials. Exercise real Object Lock/IAM under those identities with exact cleanup. Then publish a fresh bounded guest image and run the full customer Hetzner deployment/update/backup/isolated restore/cutover/revocation/usage/cleanup path. Reuse the working native scenarios; preserve the public/internal identity boundary. The Mac remains locked, so neither fresh S3 credentials nor a new device-flow approval can currently be completed through computer use.

## Remaining scope and resume commands

Protected Hetzner S3 credentials/IAM; live automated VM backups; final customer Hetzner deploy/update/restore/cutover/revocation/usage/cleanup with public ACME; independent provider/CA/bootstrap recovery material and supported pending control-state reconciliation. Existing code handles loss of fresh-provisioning credentials without blocking owned cleanup, and native restore covers original backup keys. Do not invent verification or extend automated private-key rotation beyond the original scope. Signed/public CLI distribution is complete; this is a developer preview, not a claim that the remaining live cloud proof passed. Stripe remains excluded.

Use `npm exec --yes --package=pnpm@12.3.4 -- pnpm <command>`. Source checkpoints require `check`, `format:check` and relevant integration. `node scripts/self-host-smoke.mjs --restore --wal --gateway` combines packaging/gateway/dump/WAL once. `node --import tsx recipes/smoke.mjs` uses the packaged recipes. Native VM checks run sequentially; existing `scripts/smoke-guest.ts` scenario flags select access, Compose, hosting and backups. Never run concurrent builds in the same checkout. Docker packaging uses an isolated context. `pnpm deploy --prod` must stay in an isolated copy because it can prune source devDependencies.

The final handoff/README edits are documentation-only. Check formatting and use lightweight CI; do not rerun unchanged source suites. The release tag intentionally stays at the fully checked source revision, even when main later gains documentation. All helpers are idle; bounded reviews are complete. Historical details remain in git and subsystem docs. The merged discovery helper worktree `.local/worktrees/http-discovery-mtsa70fb` is retained but inactive.

## Resources, cost and blockers

No paid resources were created this turn. `.local/hetzner-customer-read-check-2026-09-08.json` at06:53:23Z reports zero servers/IPs/snapshots/firewalls/SSH keys. All owned Docker/native backup/gateway/WAL/recipe fixtures are cleaned; shared caches remain. Preserve PG `agent-cloud-dev-postgres-1` localhost55439 and CA `agent-cloud-pki-ca-1` https://localhost:9449. Main API15176/worker15190 have older loaded code. Main DB is through0026; new0027 ran only in isolated fixtures. Do not silently migrate or replace the preserved identity.

Preserve `.local/pki`, `.local/runtime-identity`, `.local/hcloud-token`0600 for dev project15945891, and `.local/github-oauth.json`0600. GitHub app Agent Cloud Development ID3843400, public clientID`Ov23liZVS2XqBNSrJQsi`. Default Hetzner project untouched. Live published guest input1093b9f46fef0b9015c987f11f3d30e3aa2eeea168a597e02acc7eec908aa27b remains immutable; native bundlebde4feb6ec154416502ac8269a25980af2f0b499ef31656856fbd19ef858f012 is not provider-published. All prior provider snapshots were deleted; a new live customer drill needs a fresh bounded image publication.

Caps remain image VM/IP120000µUSD, customer VM/IP60000µUSD, snapshot monthly1000000µUSD. Prior internal estimate83394µUSD excludes storage and is not an invoice. No S3 cap or bucket exists. Public pricing still leaves its hourly field unresolved; monthly base is€6.49/$7.99 excluding VAT. Get the actual hourly quote before paid storage. Runtime S3 credentials must use a project different from the bucket/admin project and exact cross-project principal policy.

External blocker: Hetzner browser session expired, and one CUA check this resumed turn confirmed the Mac is still locked. One login and one unlock question are already pending; do not repeat them or retry browser loops. VM API access works. GitHub is already signed in. Earlier ambient Anthropic/Braintrust/Apple credentials were exposed through full process output; user was told to rotate, status unverified. Never reproduce values or full process args/env; inspect only PID/command name.

Retained local CLI artifact `.local/releases/cli-0.1.0-44f1662/agent-cloud-cli-0.1.0.tar.gz` is2601489bytes, SHA256`9ecddc182b515a28cb87f9dcc0ac5f6e5669c17395cc674181a0a6f942f6baea`; it is unsigned and predates the public release workflow. Keep it distinct from the verified GitHub-built public archive above. Tool binaries/logs and redacted scan reports are private ignored artifacts under `.local`.
