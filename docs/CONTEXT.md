# Current handoff

Updated 2026-09-08. Original non-Stripe goal remains active. Follow AGENTS.md and the customer-capability loop. GitHub sign-in is complete; do not ask for it again.

## What works

- First internal reference deployment is **Hetzner verified**: cheap CPX12, frontend/backend/PostgreSQL, public HTTPS, disconnect/reconnect, logs, persistent update and exact cleanup. Commits `4d2bffa` + `86b6b1b`; `docs/reference-deployment-verification.json`.
- Customer GitHub sign-in, delegated access/revocation, SSH/SFTP, durable commands, Compose, managed/custom-hostname routing, encrypted protected backups, isolated restore, network promotion and hostname movement are committed. Actual GitHub device flow passed. Application paths after the first internal deployment used native Ubuntu guests and local gateways/MinIO. See subsystem docs for guarantees; this is not final customer Hetzner/public ACME/Object Storage proof.
- Daily capture and PostgreSQL/Umami recipes are committed/pushed as `70722a43a4be02b5b145033a1f2e39f6b7d1a6d6`, Linux CI `34174591463` passed. `.local/backup-schedules-native-1.log` passed real cron after CLI exit, encrypted capture, separate-VM restore of count1/files, HTTPS cutover, new persisted write and exact cleanup. Daily timing/outage/authorization policy has DB tests; it did not wait24 hours. Recipe Docker checks passed persistence, secure admin bootstrap, visitor/event collection and isolated startup. Customer-site/native recipe verification remains.

## Current checkpoint and next outcome

Retention/purge is committed/pushed as `4ab2ee7927ef8d2eb6d09f9f91c0cb97c056569c`. CLI/API and a separate operator process preserve seven newer successful UTC scheduled recovery days, stop new restores after an authorized purge, honor Object Lock and release storage only after exact-version absence. `.local/backup-retention-smoke-1.log` passed actual CLI/encryption/MinIO, process separation, expiry, reconnect and cleanup. Required checks passed671 tests/62 files. Linux CI34176675971 failed only because a clean checkout lacked `.local` for fixture ownership records. This was reproduced as ENOENT, fixed in `scripts/support/backup-store-fixture.ts`, and actual clean-directory setup/cleanup passed `.local/backup-recovery-clean-directory.log`. The next pushed CI must verify the fix.

Operator-only blocked-upload recovery is committed/pushed as `d40644b3179417b8d5a33dd4444034213b171555`; Linux CI34177467718 passed, including the clean-checkout fixture fix. `pnpm backup:recover inspect <backup-id>` returns an immutable receipt digest; `apply <private-request.json>` verifies only the recorded upload, including after retention expires, then restores captured state/accounting and queues source staging cleanup. It never repeats capture/PUT/restore, survives original-grant revocation and retries idempotently. No reader/keyring/provider/guest runtime is loaded. Adapter recovery still checks exact identity, metadata, bytes and minimum retention; prepare/upload still demand future retention. A bounded recovery review found no blockers.

Actual `.local/backup-recovery-smoke-2.log` passed: lost real MinIO upload acknowledgement, exhausted automatic recovery, expired Object Lock, separate operator inspect/apply without reader/keyring files, repeated apply, customer CLI purge/reconnect, zero reservation and exact cleanup. Fixture archive bytes are not a PostgreSQL dump; PostgreSQL/native restore proof remains the prior native scenario. MinIO adapter proof also passed both retention modes without changing stored versions. `.local/backup-recovery-focused-1.log` passed43 focused cases. Required `.local/backup-recovery-check-final.log` passed typecheck/lint and675 tests/62 files. Formatting passed `.local/backup-recovery-format-final-2.log`; the subsequent CI directory fix has focused lint/format and actual clean-directory proof. No unchanged comprehensive rerun is needed.

The verified scratch-capacity checkpoint adds control-worker scratch preflight to capture/restore. It checks two bounded archives plus256 MiB before transfer and before consuming a work attempt. Six consecutive low-space refusals preserve attempts0 and perform no read/download/restore; restoring capacity resumes each same operation. `.local/backup-space-focused-1.log` passed all4 backup scenarios, focused lint/typecheck passed, and `.local/backup-space-smoke-1.log` passed the actual CLI/encryption/MinIO/purge path with the real filesystem check. Bounded review found no blocker. Required `.local/backup-space-check-final.log` passed typecheck/lint and676 tests/62 files; formatting passed `.local/backup-space-format-final.log`. Continue with usage and self-hosting.

Next acceptance: `acld usage` explains active VM and retained-backup reservations, account/grant limits and reservation history through resize/failure/destruction. Current allocations already retain creation/retirement and mutate their hourly reservation on resize admission/verification. Preserve those changes as reservation history; do not fabricate historical rates or describe reservations as billed charges. Follow with self-hosted installation, upgrade and control/key recovery. Hetzner Object Storage access remains externally blocked.

## Remaining scope

Protected provider captures and IAM; provider automated VM backups and remaining operational recovery checks; native/customer-site recipe verification; remaining resource limits and explainable usage intervals; HTTP command endpoints where CLI currently uses API plus SSH; self-host installation/upgrade/control-database recovery and independent key recovery; discoverable API/agent instructions; budget-appropriate operational failures; final authorized customer Hetzner deploy/update/restore/cutover/revocation and cleanup. Stripe excluded. Original plan is `docs/archive/original-plan.md`; do not restart its architecture work.

## Resume commands

Repo `/Users/yahorbarkouski/Documents/ChatGPT/learning/agent-cloud`, branch `yahor/agent-cloud`, private origin `yahorbarkouski/agent-cloud`. Use `npm exec --yes --package=pnpm@12.3.4 -- pnpm <command>`. Checkpoints require `check`, `format:check` and relevant integrations. `smoke:backup-retention` and `smoke:backup-recovery` run without VMs; `smoke:backup-store` verifies storage permissions. Both run only for code changes in CI.

For native backup/cutover: `build:guest`, then `AGENT_CLOUD_ACCESS_SCENARIO=1 AGENT_CLOUD_BACKUP_SCENARIO=1 node node_modules/tsx/dist/cli.mjs scripts/smoke-guest.ts`. Add `AGENT_CLOUD_BACKUP_SCHEDULE_SCENARIO=1` for real cron. VM smokes are sequential. Selected image `1093b9f46fef0b9015c987f11f3d30e3aa2eeea168a597e02acc7eec908aa27b`; do not patch published inputs. Main DB has0025 applied and26 hashes matched `.local/backup-purges-db-check.log`. Applied SQL is immutable.

## Resources and blockers

**No paid resources or root-owned VMs, recipe resources or S3 fixtures remain.** The bounded self-host packaging task may create separately owned local Docker fixtures and must report their cleanup. All backup-target receipts say deleted, source receipt absent, OrbStack inventory previously empty. Local retention smoke cleaned its exact container/database/private files. No Hetzner bucket was created. Dev project15945891 was last verified empty after live internal proof; subsequent work made no paid calls. Default untouched.

Retain local PG `agent-cloud-dev-postgres-1` on55439 and CA `agent-cloud-pki-ca-1` on https://localhost:9449, `.local/pki`, `.local/runtime-identity`. API PID15176/worker15190 have older loaded code; restart only when needed. Never print raw CA logs. Retain GitHub OAuth app `Agent Cloud Development`, ID3843400, public client ID `Ov23liZVS2XqBNSrJQsi`; `.local/github-oauth.json` and `.local/hcloud-token` are0600.

Caps: image VM/IP120000µUSD, customer VM/IP60000µUSD, snapshot monthly1000000µUSD. Prior internal VM/IP estimate83394µUSD excludes storage and is not an invoice. No S3 cap is recorded: obtain current pricing and record a cheap cap before paid storage.

**External action pending:** Hetzner browser tab5 remains signed out at https://accounts.hetzner.com/login, reconfirmed this checkpoint. An async login question is already pending for scoped S3 credential creation. Existing VM API token works. Continue independent work; do not repeat the question or request GitHub login.

Earlier, a full process listing exposed ambient Anthropic, Braintrust and Apple app credentials in tool output. The user was told to rotate them; rotation is unverified. Never reproduce values or use full process/argument/environment dumps. Use PID/command-name diagnostics only.
