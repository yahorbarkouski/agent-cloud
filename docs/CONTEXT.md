# Current handoff context

Updated 2026-09-07. Full cloud goal remains active, excluding Stripe. M0 is complete; M1 is close to its customer drill, and M2–M7 remain open. Work only in this standalone repository. Customers bring existing agents. Implementation, private pushes and bounded cheap Hetzner tests are authorized. No expensive plans, benchmarks, warm pool or automatic type/region fallback.

## Current checkpoint

Branch `yahor/agent-cloud`, private origin https://github.com/yahorbarkouski/agent-cloud.git. HEAD4030e9d records the prior cleanup implementation950ba8c137dc5a44e36e44646ee62dd0a4251536 and successful exact Linux CI34124600353. Current uncommitted operator recovery extends that admitted-cleanup contract. It is locally verified; formatting and final evidence review passed. Finish private commit/push and exact Linux CI.

`pnpm machine:recover inspect <cleanup-operation-id>` returns retained scope, attempts, resources, recoveries and a state digest. `apply <private-request.json>` records either explicit `close_create` evidence or one additional exact-target `retry_delete`. Requests require pre-existing customer destroy authority. They serialize under the machine lock, bind the inspected state, and atomically save authority, audit and worker wake-up. Original provider outcomes remain immutable. Closure uses the distinct `operator_closed` resolution, never fake success or absence. New inventory discoveries persist even when admission rejects the stale digest. Every historical source resource ID must be acknowledged. Retry allowances use one SQL function shared by operator admission and the worker. Exact labels, IP assignment, absence, existing backoff and terminality remain enforced.

`HetznerInventory` is the extracted read-only transport. The operator command needs no images, prices, bootstrap or signing configuration. No public HTTP route or customer privilege was added. Provider closure is an explicit attestation backed by operator-maintained evidence. Software validates its scope and provenance, not the truth of external provider confirmation. Never manufacture that evidence from empty inventory or timeouts. See `docs/architecture/operator-recovery.md`.

Full94746 passed420tests/36files, typecheck/lint,104.25s. `.local/operator-recovery-check-2.log`. Independent gpt-5.6-sol review reran33cleanup tests successfully and found no production blocker. Native cleanup60304 passed actual CLI/HTTP/PostgreSQL/Graphile cancellation with delayed simulated inventory and complete disposal. `.local/operator-recovery-cleanup-smoke.log`. Restarted API/worker CLI49678 passed create/inspect/destroy and disposal. `.local/operator-recovery-local-smoke.log`. Customer skill validation passed. Evidence preserves intermediate fixture, type and lint failures in `docs/research/m1-operator-recovery-verification.json`. No active transcript directory was supplied; the reviewer used artifacts and the decision trail.

Migration0019 applied to development after stopping exact owned PIDs38043/38044. All20 hashes match. New hash92dcf490e4a775ed21daa5a69beefc67e914d308e0f55b87201722c8562f931b is immutable. All0000–0019 are applied: do not edit them; future changes need0020. `.local/operator-recovery-migration-check.json`. New services are API42350/worker86187 on4319. SQL at13:22:12Z shows zero active allocations, simulated VMs/IPs and unfinished image builds. Actual Hetzner inventory in `.local/operator-recovery-provider-inventory.json` has zero servers, Primary IPs, snapshots, firewalls and SSH keys. No paid mutation or native VM occurred in this slice.

## Existing behavior and proof

Customer destroy cancels an active create within its original operation and ends `cancelled` after cleanup. A failed or ready terminal create retains its result while a new destroy handles cleanup. Immutable allocation-scoped authority survives grant revocation for owned deletion only. Unknown creates never retry. Duplicate IDs, original receipts and image pins survive uncertainty. VM deletion cannot implicitly auto-delete an IP while a source VM create remains pending. Customer cleanup passed407tests plus native CLI cleanup before this extension; exact CI34124600353 passed. See `docs/architecture/machine-cleanup.md` and its verification/review artifacts.

Explicit customer runtime157da958d54d87e5c15e0a6c7c8ceac01f5fe636 passed383tests and exact CI34119114439. It connects pinned signed renewal-capable images, encrypted bootstrap, enrollment, renewal, readiness and customer lifecycle jobs. Startup and exact-resource cleanup work without private guest material, current default-image availability or pricing. Fresh work checks its own dependencies. Customer power-off uses graceful shutdown and observed off. No customer Hetzner boot is yet claimed.

Renewal7843b036da6f9d3bf968366576549912f183e6af passed exact CI34115440102 and local native VM/service checks. Original P-256 signatures, database-time freshness, current owned VM/IP, pinned SSH proof, four-per-hour persisted signing attempts, immutable identity history and atomic certificate generations are implemented. Enrollment clock follow-upb16466b9624f98b889f5a41e0a289d4507c5e2a6 passed CI34116511248. See the renewal and customer-runtime architecture/evidence documents.

Corrected real Hetzner image buildde7d3f33-0f3e-4512-afc6-d163b1cd7359 passed installation, sanitation, graceful shutdown, snapshot boot, verifier enrollment/runtime and signed publication/selection. All seven owned resources and local keys were removed10:35:25Z; selection now correctly rejects its deleted snapshot. `docs/research/m1-hetzner-durability-drill.json` and `m1-hetzner-durability-release.json` retain evidence. The earlier failed buildbbb28cd4-aca1-4a94-87cf-364728bf6771 exposed empty installed files after hard poweroff; preserve that failure separately. The correction flushes the filesystem and stops the random-seed writer before sanitation. Native OrbStack virtualization overrides belong only to fixtures, never published inputs.

## Images, services and secrets

Current immutable guest manifest212a16f8a29089b2ed0a18c9f9bb91e188411c729f566efcea71d59dadb15424; versiondev-a170e62cde5f91fe03baca10; public input digest a170e62cde5f91fe03baca10cde0c3605769c6632e0eb1ca39e1ca21d57d34e0; transfer checksum86fb552a5714e16f3e9249220836c335e97aae4da7b7082566a8483e16f0f667. `.local/guest-build.json` selects `.local/guest-builds/<manifest>`. Recovery code does not alter image inputs. The historical provider release used an older enrollment-only image, so it cannot satisfy customer selection even if retained. No deployable snapshot or customer firewall exists now.

Commands use `npm exec --yes --package=pnpm@12.3.4 -- pnpm <command>`. Pnpm uses Node24.10.0; system Node26.5. TS scripts importing src/\*.js need tsx. PostgreSQL17 is127.0.0.1:55439, database/useragentcloud with passwordlocal-development-only. Smallstep CA https://localhost:9449 uses256MiB/.5CPU and pinned step0.30.6/step-ca0.30.2. `.env` remains simulated. Current API42350/worker86187 logs are `.local/simulated-api.log` and `.local/simulated-worker.log`.

Provider projectagent-cloud-development15945891; Default untouched. `.local/hcloud-token`, `.local/admin.credentials.json` and identity/PKI secrets are0600. Never print them or put tokens in argv/guest data. `.local/runtime.json` remains image_factory. Preserve `.local/runtime-identity` private material. Public release key ID6c4eb920bf75d1b753253c327082b3c1386136928830e50779b9efc2e1c1a481. Policy reloads for every authorization; restart API and worker after private changes. No quick tunnel, image worker/API or native VM is running. Do not reuse a stopped tunnel URL.

## Next work

Finish the operator recovery checkpoint, then run the bounded customer drill. Build and temporarily retain a renewal-capable release, create owned customer firewall rules, use reachable HTTPS, read fresh inventory/pricing and record exact CPX12 nbg1 spending/deadline caps. Own cleanup throughout. The prior image drill used one build,90minutes,$0.12 VM/IP gross and$1 monthly snapshot caps; these were caps, not invoices. Start no warm pool. Image cancellation and exact-build cleanup remain available without signing keys, source inputs or pricing.

Continue M2–M7 customer access/device login, persistent commands/file transfer, Compose deployment, HTTPS/routes, recipes/backups/restore, usage and self-hosting. Image and lifecycle proof do not complete the application product. PROGRESS and DECISIONS retain earlier checkpoints; this file keeps only current execution context.
