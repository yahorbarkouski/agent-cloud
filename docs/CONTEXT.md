# Current handoff context

Updated 2026-09-07. The active goal is the full agent-operated cloud in `docs/archive/original-plan.md`, excluding Stripe. M0 is complete; M1 and M2–M7 remain open. Do not mark the goal complete after an image or foundation checkpoint.

## Scope and repository

Customers bring Codex/Claude Code or another coding agent. Build their CLI/API/skills, not an agent. TypeScript, ordinary Linux VMs, Docker Compose, SSH and Hetzner. Keep costs low, with no benchmarks, warm pool or automatic type/region fallback. User authorized continued implementation, private GitHub pushes and Hetzner setup. No paid resource has been created.

Repository `/Users/yahorbarkouski/Documents/ChatGPT/learning/agent-cloud` is standalone inside an unrelated workspace. Work only here. Branch `yahor/agent-cloud`; private origin `https://github.com/yahorbarkouski/agent-cloud.git`. Authenticated-builder implementationd2e12f9808c0567851131acbcab987589aca688c is pushed. Linux CI34086739961 passed in2m33s with frozen install, full checks, formatting, fresh PKI, all three native smokes and cleanup. This documentation checkpoint records those results. Earlier transport/journal evidence is in PROGRESS and research artifacts. This goal turn made progress; it is not blocked.

## Implemented foundation

Contracts/Zod, PostgreSQL/Drizzle, Hono/Graphile, SDK and JSON CLI. Tenant grants/delegation/revocation, idempotency, quotas, gross currency/VM/IPv4 prices and pinned offers. Customer journals persist intent before I/O, own IP before VM, reconcile uncertain creates without resubmission and require VM/IP absence before releasing reservations.

Migrations0006–0008 implement encrypted allocation bootstrap, immutable claimed/issued guest identities and durable bounded certificate attempts. Enrollment proves the directly observed provider address with pinned SSH before signing. Certificate persistence, token erasure and runtime handoff commit together. Runtime uses a five-minute certificate forcing only `sudo -n -- /usr/local/bin/guestctl inspect --json`, then checks components, disk, pinned image and boot ID. Reboot/power-on require changed boot ID. Fresh paid effects and completion enforce a30-minute operation deadline. Production `createTasks` still does not supply GuestProvisioning; live startup is gated.

Guestctl and the pinned Ubuntu installer boot/enroll through actual cloud-init/systemd locally. Sanitation, interruption recovery, allocation/Docker data refusals and distinct clone identities have native proof. Format2 provenance binds the entire public input tree. The controller's transfer preflight checks the admitted checksum with base OS tools before uploaded code executes. Signed Ed25519 release metadata authenticates recorded claims, not current provider ownership or actual snapshot boot. See the guest/image/runtime architecture documents and PROGRESS for historical evidence.

## Operator image build implementation

The sibling SQL journals never fabricate customer allocations. Migrations0009–0012 preserve admission, effects and resource identities, exclude customer/operator ownership collisions, permit exact-ID retries after missing actions, and require the original effect UUID in observed resource labels. All 14 migrations through 0013 are applied with verified hashes. Never rewrite applied migrations.

`image-budget.ts` reserves two full VM/IP lifetimes with per-resource whole-hour rounding and separate gross snapshot storage. It bounds build deadlines to24h and retention to30days; aggregate open builds hold their full caps under lock78131026. `image-builds.ts` owns actual-input admission, replay, inspection and cancellation. Effect and resource journals commit intent before submission, reconcile uncertainty, retain every duplicate ID, and never retry a create. Exact-ID stop/delete supersession preserves original receipts. `image-cleanup.ts` reconciles before selecting one owned deletion; full abort includes the unpublished snapshot. Successful retention cleanup remains unfinished.

The Hetzner image/access transport is implemented. It receives the persisted effectId, checks action association, validates exact resource observations and checks the pinned compatible Ubuntu base before fresh creates. `imageEffectLabels` binds creation, lookup, observations, dependency/deletion checks and SQL ownership to the effect UUID. Missing actions differ from running/failed; authoritative desired state can finish stop/delete, otherwise only exact-ID retry is permitted. Snapshot actions target the source server while their receipt owns the image ID.

Current new code:

- `apps/control/src/image-access.ts` generates independent native Ed25519 management/host keys and atomically publishes owner-only per-build directories. Recovery validates owner/mode/symlink boundaries, private/public correspondence and the exact admission. SQL holds public keys and secret references. Default `.local/image-access`, override `IMAGE_ACCESS_DIRECTORY`.
- `image-renderer.ts` requires the exact active prepared effect and confirmed dependencies, recovers keys, then rechecks cancellation/deadline. Cloud-init creates the dedicated builder account, supplies only its disposable host private key, and records build/effect/manifest. `image:build prepare` validates inputs and returns public admission configuration without DB/provider credentials; fresh admit requires the matching local key store. Replay/inspect/cancel remain resource-free. No live advancement command yet.
- `packages/remote/src/image-builder.ts` implements pinned native SSH/SFTP, trusted transfer preflight, atomic root-owned installation-start marker, completed-receipt recovery and sanitation. Concurrent/repeated requests cannot rerun the installer. `started` does not prove whether a disconnected process is running; partial installation requires disposal or explicit recovery. The durable controller now persists phase intent/receipts and serializes work against current provider ownership.
- `ssh-files.ts` shares isolated SSH file/options setup with the existing read-only probe. Pinned host keys and host CA keys retain distinct contract schemas. Builder management does not grant the runtime probe arbitrary commands, Docker or general sudo.
- Guest sanitation removes builder sudo/drop-in/state/boot records before erasing host keys and homes. A lost response after access erasure cannot authorize publication. Terminal owner-checked operator-key cleanup is connected to the full-abort runner.

## In-progress controller work

This goal turn made progress after `8eb2a46`. The dirty controller now connects `image-release-plan.ts`, `advance-image-build.ts`, `image-builder-work.ts` and terminal access removal. The pure planner reads one consistent snapshot; executors acquire their own build locks. The runner creates access resources and a builder, persists SSH phases, requires saved sanitation before stop/snapshot, and returns `verification_required`. Full abort covers every implemented stage and removes local keys only after authoritative provider cleanup. No live advancement CLI or verifier/promotion exists yet.

Migration `0013_image_builder_work` is now APPLIED to the base development DB. All 14 migration hashes match disk (read check `f60973`). Never rewrite it. SQL enforces immutable builder/effect ownership, exact JSON and receipt shapes, forward-only installing→installed→sanitizing→sanitized progress, active admission before fresh SSH work and saved sanitation before stop/snapshot. A lost completed install response recovers from guest evidence; unknown or interrupted sanitation requests full cleanup.

Full check `85297` passed 288 tests across 26 files, typecheck and lint. Earlier targeted `38406` passed 94 tests across four files. Initial checks exposed validation-order expectations and lint in fixture promises; both are corrected. Reviewer `/root/image_release_judge` completed the controller/access/schema review: prior P2 accepting extra SQL JSON fields is fixed and covered; no new blocker. Report `docs/research/m1-image-builder-work-review.md`. Formatting 14512 and native proof passed; final trail review and Linux CI are pending.

Native `smoke:builder` **6670 PASSED**. It used the durable controller, protocol-provider wrapper and real native SSH/SFTP, deliberately lost an installed response, recovered through a new SQL connection, then verified sanitation, stop/snapshot ordering, two clone boots and full abort/key cleanup. Both clones passed enrollment, runtime service/disk failures, reboot and distinct identity checks. Builder scan 64 files/94,289 bytes and both clone scans (125 files each) had no matches. Final cleanup checks `d10d87`/`bf26c2` at 06:07:53Z confirmed an empty OrbStack inventory, absent ownership records and empty private fixture directory. No VM or smoke remains.

Selected verified manifest `03ef1c1b04ce408d298922c4d299d076a7ec8dd9b6e54a0d7dcf303656568474`, version `dev-42712ace9ccfcbdc546de157`, inputs `42712ace9ccfcbdc546de15745da6feabaeb6a06131e421070320ac7ccdd91a0`, checksum `be4a1d280b645a7c31c3620cb87a4effd77997be3a74480da93b1ab1297eb72d`. Only guestctl.mjs changed versus the previous input tree because new shared contracts enter its bundle. Evidence: `docs/research/m1-image-builder-work-verification.json`.

Base API/worker were stopped by confirmed owned PIDs/cwd before migration, then restarted from checked build: API **62811**, worker **28742**. Direct CLI smoke **68795 passed** create/inspect/destroy/cleanup for `vm_2078205a-a653-4de6-8b22-7e28daf856ff`; the provider remains simulated. No cloud resource was created. SQL cleanup check45f8f1 confirms zero active allocations/simulator VMs/IPs. Complete final review, commit/push and Linux CI, then proceed to platform verifier lifecycle. The full goal remains active.

## Previous authenticated-access verification

Evidence: `docs/research/m1-image-access-verification.json`. Full29948 passed256tests/24files, typecheck and lint. Formatting2331 passed after a second pass on one method chain. PKI61632, SSH21584 and enrollment58558 passed with actual Smallstep/OpenSSH/TLS and their rejection checks. Shell syntax passed. No further code changes followed those successful checks.

Native `smoke:builder`63643 completed successfully. It verified actual cloud-init/schema, pinned first SSH, SFTP, wrong identity/expiry/tampered code refusal, concurrent installation, receipt recovery without the uploaded installer, sanitation and two fresh clone boots. Both clones passed enrollment, runtime service/disk failure recovery, reboot and bootstrap-token scanning. Their machine IDs and SSH/TLS identities differ. Builder key scan64files/94289bytes had no matches; clone scans125files each had none. Cleanupdea8c1 confirmed empty local VM inventory, absent ownership records and empty private fixture access directory.

Native findings were fixed, not bypassed: cloud-init rejects empty ssh_genkeytypes; explicit ssh_keys maps suppress unintended generation. Fixture-only growpart/resizefs are disabled because OrbStack owns disk sizing. Staging uses the account's numeric GID instead of assuming its group name. Installed tools use absolute /usr/local/bin paths under the restricted SSH PATH. Shared trust validation now accepts the existing ECDSA CA through its proper schema. Failed runs25078/13494/89933 and their owned VM/scratch were cleaned. All decisions are in DECISIONS.tsv.

Reviewer `/root/image_release_judge`, gpt-5.6-sol, completed the final access/render/SSH and artifact/trail review, including all native fixes and successful verification. No blocker remains in this checkpoint. Report: `docs/research/m1-image-access-review.md`. No active-workspace transcript directory was supplied; reviews are artifact/trail audits. The implementation is committed, pushed and verified by Linux CI. Continue the actual controller after this documentation checkpoint.

## Local runtime and secrets

- Run `npm exec --yes --package=pnpm@12.3.4 -- pnpm <command>`. Node24 intended; system26.5/pnpm runtime24.10.0. Avoid concurrent builds sharing output.
- Docker/OrbStack healthy. PostgreSQL17 project `agent-cloud-dev`, `127.0.0.1:55439`, db/user `agentcloud`, password `local-development-only`. Tests create/remove isolated databases. Current API session62811 on127.0.0.1:4319; worker28742; simulated provider. Inspect process command/cwd before stopping.
- `.env` is simulated. `.local/admin.credentials.json` and `.local/hcloud-token` are0600. Never print secrets or pass them in argv/guest data.
- Smallstep `.local/tools/step-0.30.6`; CAproject `agent-cloud-pki` at https://localhost:9449. Step-ca0.30.2 is digest pinned,256MiB/.5CPU. Issuer state `.local/pki/issuer`; root private key and provisioner password outside its container mount. Public trust `.local/pki/public`. Preserve keys. Host/TLS1h, probe5m, TLSserveronly. Renewal/production recovery remain unfinished.
- No local VM or VM smoke remains; builder6670 passed and cleaned up. Ownership records are `.local/guest-image-builder.json`, `guest-image-refusal.json`, `guest-image-machine.json`. Builder/image smokes share records; run them sequentially. Leave active VM smokes alone because even diagnostics can restart a sanitized source. On failure inspect/delete only recorded machines and exact private fixture access before retrying.
- Current verified input selection is listed in the controller checkpoint above. Inputs are immutable `.local/guest-builds/<digest>`, files0444/directories0555. Capture `.local/guest-build.json` once and verify before use.

## Hetzner and next work

Verified account, dedicated project `agent-cloud-development`15945891; Default untouched. RW token saved privately. Last real read-only check42001 at04:17:12Z found compatible x86 Ubuntu24.04 image161547269,5GB,and zero servers/IPs/snapshots/firewalls/keys. Evidence: `docs/research/hetzner-image-transport-read-check-2026-09-07.json`. Historical account pricing was USD/23%VAT, CPX12 in nbg1/fsn1/hel1 at27798grossmicroUSD/hour including IPv4, snapshot24477microUSD/GB-month. Refresh before paid mutation; these observations are not current admission authority. A90-minute two-machine envelope reserved4billable VM/IP hours plus40GB full-month storage,1.090272USD before margin. A deadline cannot force provider deletion during an outage.

1. Finish verifying the connected durable builder controller and terminal key cleanup after authoritative temporary-resource cleanup. Keep sanitation receipt persistence before any stop/snapshot. `runImageEffect` and `planImageCleanup` already acquire their own session locks. Do not call them from inside another connection's build lock; it would return busy. Runtime work needs the same per-build lock and a fresh ownership/phase check before SSH. No paid resource until a complete bounded cleanup path exists.
2. Implement platform verifier bootstrap/enrollment, actual stopped snapshot publication and distinct boot proof, signed retained-release promotion/consumption. Separate successful retention cleanup from full abort. Finish renewal/operator recovery and production GuestProvisioning wiring, then one bounded cheap Hetzner drill with cleanup.
3. M2 device/browser login and customer SSH access; M3 transfers/durable commands/Compose/routes; M4 recipes/off-VM backup/isolated restore; M5 usage/traffic/alerts/cost limits; M6 self-hosting; M7 real end-to-end/failure proof. Keep the full goal active.
