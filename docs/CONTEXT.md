# Current handoff context

Updated 2026-09-07. The active goal is the full agent-operated cloud in `docs/archive/original-plan.md`, excluding Stripe. M0 is complete; M1 and M2–M7 remain open. Do not mark the goal complete after a foundation checkpoint.

## Scope and repository

Customers bring Codex/Claude Code or another coding agent. Build their CLI/API/skills, not an agent. TypeScript, ordinary Linux VMs, Docker Compose, SSH and Hetzner. Keep costs low, with no benchmarks, warm pool or automatic type/region fallback. User authorized continued implementation, private GitHub pushes and Hetzner setup. No paid resource has been created.

Repository `/Users/yahorbarkouski/Documents/ChatGPT/learning/agent-cloud` is standalone inside an unrelated workspace. Work only here. Branch `yahor/agent-cloud`; private origin `https://github.com/yahorbarkouski/agent-cloud.git`. Latest implementation1e7bdfffe3a386d3baf07971edf2dd6fec34a30d is pushed. Linux CI34083050344 passed in2m27s with frozen install,232tests,typecheck/lint/format,fresh PKI and three native smokes/cleanup. This documentation checkpoint records those results. Historical journal/provenance evidence belongs in PROGRESS and research artifacts.

## Implemented foundation

Contracts/Zod, PostgreSQL/Drizzle, Hono/Graphile, SDK and JSON CLI. Tenant grants/delegation/revocation, idempotency, quotas, gross currency/VM/IPv4 prices and pinned offers. Customer journals persist intent before I/O, own IP before VM, reconcile uncertain creates without resubmission and require VM/IP absence before releasing reservations.

Migrations 0006–0008 implement encrypted allocation bootstrap, immutable claimed/issued guest identities and durable bounded certificate attempts. Enrollment proves the directly observed provider address with pinned SSH keys before signing. Certificate persistence, token erasure and runtime handoff commit together. Runtime uses a five-minute certificate forcing only `sudo -n -- /usr/local/bin/guestctl inspect --json`, then checks components, disk, pinned image and boot ID. Reboot/power-on require changed boot ID. Fresh paid effects and completion enforce a 30-minute operation deadline. Production `createTasks` still does not supply GuestProvisioning; live startup is gated.

Guestctl, pinned Ubuntu installer and public image build work locally. Full sanitation, interruption recovery, data refusals and two fresh clone identities passed actual OrbStack Linux checks. Format 2 provenance binds the entire public input tree. The controller-owned transfer preflight verifies hashes before uploaded code executes. Signed Ed25519 release metadata authenticates recorded claims; it does not independently establish live provider ownership or actual snapshot boot. Native evidence: `docs/research/m1-image-provenance-verification.json`.

## Current operator image journal work

The transport checkpoint1e7bdfffe3a386d3baf07971edf2dd6fec34a30d is committed and pushed. Linux CI34083050344 passed in2m27s: frozen install,232tests,typecheck/lint/format,fresh PKI,three native smokes and cleanup. This documentation checkpoint preserves that evidence. New `packages/hetzner/src/image-{release,resources}.ts` implements image/access HTTP and observations. ImageProvider receives persisted effectId on submission, checks action target association and reads pinned base images. Fresh creates require compatible Ubuntu24.04. Missing actions can finish from desired state or supersede only an exact-ID stop/delete; creates never retry. Snapshot actions target the source server while receipts own the image. Unavailable snapshots remain readable but cannot confirm success.

Review caught build/role-only ownership. `imageEffectLabels` now binds submission, lookup, candidate checks, observations, final matching, fresh dependencies and deletion policy to the create effect UUID. Wrong/missing labels cannot confirm an unknown create; accepted receipts stay unverified and cannot authorize deletion. New migration0012 enforces new observation labels. Reviewer `/root/image_release_judge`, gpt-5.6-sol, marked the finding fixed and independently passed65focused tests. Report `docs/research/m1-image-transport-review.md`. Only the saved OpenAPI under `.local/hetzner-openapi.json` is ignored; the read-check JSON under docs/research is committed evidence.

Final full check85939 passed232tests/22files,typecheck/lint. Its formatting check needed a method-chain correction96280d; final format73724passed. Both0011/0012 are applied;766251 confirmed both hashes,13migrations and zero open image builds/active allocations/simulated servers/IPs. Local smoke12358 passed CLI/API/worker create/inspect/destroy/cleanup for vm_c46d46b0-eaa1-477f-a22f-8f3a4aa07c3d after0011;0012 is image-only and covered by the final suite. Evidence `docs/research/m1-image-transport-verification.json`. Real read-only check42001 found compatible x86 Ubuntu24.04 image161547269,5GB,and zero servers/IPs/snapshots/firewalls/keys at04:17:12Z; evidence `hetzner-image-transport-read-check-2026-09-07.json`. No paid mutation or guest-source change. No builder secret/SSH implementation yet.

Current implementation includes:

- `packages/contracts/src/image-build.ts` and `image-provider.ts`: admission, budgets, resource roles, strict commands, receipts, observations and one-way resolutions. Provider server metadata deliberately has no invented SSH-key IDs. Firewall observations retain rules/attachments.
- Migration 0009: sibling `image_builds`, `image_build_effects`, `image_build_resources`, immutable ownership/history, cleanup guards and cross-journal identity locks. Migration 0010 extends customer identity exclusion to updates. Neither builder nor verifier fabricates a customer allocation.
- `apps/control/src/image-budget.ts`: exact offer/component-price checks, two full VM/IP lifetimes rounded separately to hours, gross snapshot storage, 24-hour maximum build deadline and at most 30-day retention. Aggregate open builds reserve their full caps under transaction lock 78131026. `image-builds.ts`: actual input-byte validation, idempotent admission, consistent inspection and cancellation.
- `image-effect-journal.ts`: session lock per build, commit-before-submit, immutable receipts, unknown-create reconciliation, all duplicate IDs retained. Duplicate history remains unresolved even after known candidates are removed. Exact-ID stop/delete retries link the old pending outcome to a prepared identical replacement; creates cannot supersede. The action status is checked before cleanup of accepted-create duplicates; already recorded absent receipt identities are not reclaimed.
- `image-effect-policy.ts` checks owned dependencies, pinned offer/access, sanitation receipt before stop and observed stopped source before snapshot. `image-resources.ts` records observations and validates created configuration. `image-cleanup.ts` selects one owned deletion, handles duplicates, orders servers before dependencies, and retains reservations while creates remain uncertain. Current cancel means full abort, including an unpublished snapshot. Retained successful-release cleanup is unfinished.
- `pnpm image:build admit <config.json> | inspect <build-id> | cancel <build-id>` is implemented in `scripts/image-build.ts`. Admission reads fresh Hetzner pricing but creates no cloud resource; replay/inspection/cancel do not need provider credentials. `packages/hetzner/src/image-pricing.ts` reads gross snapshot pricing. The transport library is implemented; no live CLI advance command is exposed yet. The operator configuration requires preexisting public keys/secret reference; key generation and SSH rendering are next.

Verification complete for this checkpoint:

- Final full check31120 passed194tests/21files, typecheck and strict lint. Formatting passed29387 after correcting one test file. Both unknown and accepted-receipt duplicate cleanup regressions pass. Exact evidence: `docs/research/m1-image-journal-verification.json`.
- Post-migration restarted actual CLI/API/worker smoke47670 passed for `vm_e1ec62a2-9165-477c-befd-97f8fb223b42`, with cleanup. Provider simulated. At that earlier checkpoint the base DB reported11migrations and zero active allocations/image builds/simulated servers/IPs. Both applied0009/0010 hashes match their files. The scope-update trigger is in follow-up0010; applied0009 remains INSERT-only.
- gpt-5.6-sol reviewer `/root/image_release_judge` identified unknown duplicate cleanup, exact-ID retry, then accepted-receipt duplicate cleanup. Fixes and regressions are implemented. Final re-review marks every code finding fixed in `docs/research/m1-image-journal-review.md`. Parent refreshed the final test/migration context after the audit. No active-workspace transcript directory was supplied; reviews are artifact/trail audits.
- DECISIONS and PROGRESS document both the earlier193-test check and final194-test result. Final evidence, CONTEXT and review are updated. Implementation push and Linux CI are complete. No guest source changed, so native guest smokes were not repeated locally. Linux CI passed all three existing native checks with fresh PKI setup.

## Local runtime and secrets

- Run `npm exec --yes --package=pnpm@12.3.4 -- pnpm <command>`. Node24 intended; system26.5/pnpm runtime24.10.0. Avoid concurrent builds sharing output.
- Docker/OrbStack healthy. PostgreSQL17 project `agent-cloud-dev`, `127.0.0.1:55439`, db/user `agentcloud`, password `local-development-only`. Tests own isolated temporary databases. Current API session66019 on127.0.0.1:4319; worker77922; simulated provider. Inspect process command/cwd before stopping.
- `.env` is simulated. `.local/admin.credentials.json` and `.local/hcloud-token` are0600. Never print secrets or pass them in argv/guest data.
- Smallstep `.local/tools/step-0.30.6`; CAproject `agent-cloud-pki` at https://localhost:9449. Step-ca0.30.2 is digest pinned,256MiB/.5CPU, NET_BIND_SERVICE only. Issuer state `.local/pki/issuer`; root private key and provisioner password outside the container mount. Public trust `.local/pki/public`. Preserve keys. Host/TLS1h, probe5m, TLSserveronly. Renewal and production recovery remain unfinished.
- No local OrbStack VM exists. Smoke ownership records: `.local/guest-image-builder.json`, `guest-image-refusal.json`, `guest-image-machine.json`. Leave active VM smokes alone; even diagnostics can restart a sanitized stopped source. On failure inspect/delete only recorded machines before retrying.
- Selected public build manifest `7792e6add5b2e008c3b8e2e9bd681f4909c4e221bb43fcd66b2896f76ccf0db1`, version `dev-ec3deebee0cd9c819a50eb27`, transfer digest `9e365a2c22e2bb16db47848103bb13912b296a667a769c7099a4ce6b137387cf`. Inputs reside in `.local/guest-builds/<digest>`, files0444/directories0555. Read `.local/guest-build.json` once and verify before use; same-owner chmod is still possible.

## Hetzner and next steps

Verified account, dedicated project `agent-cloud-development`15945891; Default untouched. RW token saved privately. Read-only evidence `docs/research/hetzner-image-catalog-check-2026-09-07.json` found zero servers/IPs/snapshots/firewalls/keys. USD/23%VAT, CPX12 in nbg1/fsn1/hel1 at27798grossmicroUSD/hour including IPv4. Snapshot24477microUSD/GB-month. These are historical observations, not current admission authority. A90-minute two-machine envelope reserves4billable VM/IP hours plus40GB full-month storage:1.090272USD before explicit cap margin. Outages can extend billing beyond deadlines; never claim a cap forces deletion.

1. Transport implementation, review, docs, migrations, push and Linux CI are complete. Continue with per-build owner-only secret storage/key generation, exact prepared-attempt cloud-init rendering and authenticated first SSH/upload/install. The existing `readPrivateFile` uses O_NOFOLLOW, owner/mode/size checks. Native ssh-keygen generates independent management/host keys and verifies private/public correspondence. Keep private keys outside SQL and pass only secret references; no extra credential from the user is needed. No paid resource until complete bounded ownership and cleanup are available.
2. Platform verifier bootstrap/enrollment, actual stopped snapshot publication and distinct boot proof, signed retained-release promotion/consumption. Keep successful retention cleanup separate from full abort. Finish renewal/operator recovery and production GuestProvisioning wiring, then one bounded cheap Hetzner drill with cleanup.
3. M2 device/browser login and customer SSH access; M3 transfers/durable commands/Compose/routes; M4 recipes/off-VM backup/isolated restore; M5 usage/traffic/alerts/cost limits; M6 self-hosting; M7 real end-to-end/failure proof. Keep the full goal active.
