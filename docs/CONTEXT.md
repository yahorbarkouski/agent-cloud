# Current handoff context

Updated2026-09-07 after the live drill. Full cloud goal remains active, excluding Stripe. M0/M1 are complete; M2–M7 remain open. Customers bring existing agents. Implementation, private GitHub pushes and bounded cheap Hetzner tests are authorized. No expensive plans, benchmarks, warm pool or automatic type/region fallback.

## Root checkpoint and live proof

Root branch `yahor/agent-cloud`, private origin https://github.com/yahorbarkouski/agent-cloud.git. HEAD224a56a09e1868b8acfa2b315e9da951560c65f4 is pushed and exact Linux CI34132861071 passed on retry. It fixes native certificate keyscan testing after firewall2276c43 and provider parser6ad8e7e, both separately verified. Root currently has documentation/evidence changes awaiting final review and commit. Product source still matches224a56a.

Third customer driver67479 exited0. VM164976022/IP148484586, machinevm_1562f66a-5afe-42f4-9fcd-b8b2239b203f, allocationalloc_c2f3e074-488c-49ba-912f-0ca9115884d1 passed create/replay, signed snapshot boot, enrollment/readiness, graceful power-off/on and reboot with new boot IDs. Actual timer renewal issued15:15:25.006Z after2101271ms, and both newly served certificates matched15:15:31Z. Original public keys/CSR were preserved; `m1-customer-live-key-continuity.json` records equality and hashes derived from the checksum-verified private archive and matches both certificate generations to live observations. No private key was extracted. TLS observation proves the trusted server leaf, not an authenticated application request.

Destroyop_4bcadaef-97e4-4ff7-af4c-3254acaf4fae completed15:16:30Z. Finalizer38281 exited0, independently checking exact VM/IP absence, cleaning build653dd67f-c002-4796-b894-27865aed9ec2/snapshot429179158, deleting exact unattached owned firewall11587191 and confirming all project resources and SQL reservations zero15:17:06Z. All six temporary API/worker/tunnel PIDs stopped15:18:53Z. Owned live DBagentcloud_live_2224a90aacad42f49081e8fc08822ff7 was archived privately, pg_restore --list checked, and dropped15:19:36Z. No test VM, IP, snapshot or free firewall remains. Archive inspection is not restore proof.

Canonical public evidence: `docs/research/m1-customer-live-{verification,lifecycle,cleanup,release,process-cleanup,database-cleanup}.json` and final cleanup review. Private raw records/logs retain the `.local/customer-drill-` prefix. The207426-byte `.local/customer-drill-database.dump` is0600, SHA256d6e4c65c2747d1af8ad78f3b6c28ef39e7bc4e0e16d70b559da18a715574ac03. Never commit it or credentials. Both prior customer attempts are also fully cleaned, with parser/keyscan failures preserved separately. Customer VM/IP estimated total56826µUSD stayed under60000µUSDcap, not an invoice claim. Image VM/IP cap120000µUSD and snapshot monthly cap1000000µUSD were separate.

## M2 isolated implementation

Worktree `.local/customer-ssh`, branch `yahor/customer-ssh`. Authority checkpoint2ece39a4304f8c27ddba92e5bdf5207b32474cd7 is committed/pushed but not yet integrated into root. Local fullcheck7876 passed429tests/36files, typecheck/lint,102.76s. Eleven targeted tests cover clock skew, earliest ancestor, concurrent revocation during a delayed read, expiry during traversal and32/33depth. Independent gpt-5.6-sol review resolved the initial multi-statement snapshot issue. Exact Linux CI34136058956 passed full checks/format/CLI cleanup/fresh PKI/native SSH/enrollment/disposal on retry; first attempt stopped before tests on registryHTTP500. Root `.local/m2-authority-*` logs and CI JSON retain both outcomes.

Selected design `docs/architecture/customer-ssh.md` comes from three grounded candidates and cross-model judgment. Existing grants authorize the first slice; device/browser login follows later. Client-generated ephemeral key and hash-only ticket, native SSH ProxyCommand with a private profile, a gateway with only narrow control-RPC authority, separate customer signer/user, allocation-specific host trust, signed access-capable image and bounded monotonic revocation are the chosen shape. Gateway leases must subtract RPC latency. Admission/claim/finalization keep account-lock revalidation; an unlocked read snapshot does not prevent later revocation.

Uncommitted isolated work adds contracts/access.ts, branded AccessSessionId, session table/schema, draft0020 and four passing key/URL/deadline/claim boundary tests. Closed connection state retains its prior claim receipt. Generated migration needed allocation uniqueness before its FK; custom immutable state/attempt/claim guards and signing-attempt audit are now appended. Typecheck and15targeted auth/contracts tests passed while migrating disposable test databases with0020. Transition/race behavior has no SQL tests yet and is not ready. No access HTTP API, signer, gateway or CLI exists yet. No0020 has been applied to root/main DB. All0000–0019 are immutable.

Next: finish and commit root M1 evidence, integrate only verified authority2ece39a while preserving root M1 context/progress, then continue isolated session SQL transition/race tests and code review. Do not accidentally merge the uncommitted schema draft as complete. The sole canonical append-only decision trail remains root `docs/DECISIONS.tsv` until integration. M2 worktree has an inherited old trail; never replace newer root history with it.

## Services and credentials

Main simulated API42350/PID47026 and worker86187/PID47027 remain on4319, logs `.local/simulated-{api,worker}.log`. All temporary image/customer services and tunnels are stopped; their URLs are stale. No native OrbStack VM remains. PostgreSQL17 runs127.0.0.1:55439; the shared development DB isagentcloud. Smallstep CA https://localhost:9449 remains local with256MiB/.5CPU and pinned step0.30.6/step-ca0.30.2. No paid control-plane VM exists.

Provider projectagent-cloud-development15945891; Default untouched. `.local/hcloud-token`, admin credentials and runtime/PKI private material remain0600. Never print tokens or pass them in argv/guest data. `.local/runtime.json` remains image_factory; original keys are unchanged. Current immutable manifest739d94533985de55963de5486a656ce8ceff67cdd3b2ab7f0e593177f31bc010 and dev-aec4ae5feb7aefb4e58e4519 inputs remain intact, but their provider snapshot is deleted. M2 needs a new verified access-capable image later.

Commands use `npm exec --yes --package=pnpm@12.3.4 -- pnpm <command>`. Pnpm uses Node24.10; system Node26.5. TS scripts importing src/\*.js needtsx. Tests own unique PostgreSQL databases and must dispose them. Keep source/code checks in the isolated checkout until planned integration. Full product work still includes customer access/device login, durable runs/files, Compose/HTTPS/routes, PostgreSQL/analytics, backup/restore, usage and self-hosting.
