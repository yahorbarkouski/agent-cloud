# Current handoff context

Updated 2026-09-07. The full cloud goal remains active, excluding Stripe. M0 is complete; M1 and M2–M7 remain open. Work only in this standalone repository. Customers bring their own agents. Use TypeScript, ordinary Hetzner VMs, Compose and SSH. No benchmarks, warm pool, expensive plans or automatic fallback. Implementation, private pushes and bounded cheap cloud testing are authorized.

## Current renewal checkpoint

Branch remains `yahor/agent-cloud`. Renewal is committed and pushed as `7843b036da6f9d3bf968366576549912f183e6af`. Exact CI34115440102 passed, including fresh PKI/native smokes/cleanup; `.local/renewal-ci-result.json`. Full goal remains active. Customer runtime is still disabled.

Implemented `/guest/renew`, original P-256 signed proof with database-time freshness, current owned VM/IP and pinned SSH proof, four persisted signing attempts/hour with thirty-second cooldown, immutable issued renewal history, complete certificate generations with atomic `current` pointer, activation retry, bounded staging cleanup and persistent boot/calendar timer. Renew reloads existing SSH/Caddy configuration. Initial enrollment identity remains immutable. Historic enrollment-only input inventories remain readable; customer mode must require renewal units. See `docs/architecture/guest-renewal.md` and `docs/research/m1-guest-renewal-verification.json`.

Full check77578 passed368tests/34files, typecheck/lint,92.88s. Native3342 passed real Smallstep/SSH/TLS renewal, lost response, saved identity replay, installation and activation retry. It uses fixture-aged issuance metadata, not elapsed expiry. Native VM68924 passed installed timer, actual service reload, health, bootstrap erasure/reboot and complete cleanup. All owned VM records are absent. Earlier native82526 passed before reload-only changes; earlier full41735 failed because OpenSSL refused negative days in a fixture, then was corrected. gpt-5.6-sol review found no material blocker after owned staging cleanup was added. Artifact/trail review only.

Migration0017 is now applied and immutable, hashf2039f86b6843aef9a1141fa6d79a56ae2d26dae54403edfcfed254c0a102e56. All18hashes match. Simulated API15970 and worker63771 were restarted after migration; local CLI smoke29835 passed create/inspect/destroy/cleanup. SQL confirms zero active allocations, unfinished image builds and simulated servers/IPs. Logs `.local/renewal-migrate.log`, `.local/renewal-migration-check.log`, `.local/renewal-local-smoke.log`. Original API94916/worker74632 stopped. No paid resources were created for renewal. Final formatting passed. Renewal commit and exact CI passed. A small enrollment-clock follow-up is now in progress before customer runtime work.

Current immutable input manifest212a16f8a29089b2ed0a18c9f9bb91e188411c729f566efcea71d59dadb15424, versiondev-a170e62cde5f91fe03baca10, digest a170e62cde5f91fe03baca10cde0c3605769c6632e0eb1ca39e1ca21d57d34e0, transferchecksum86fb552a5714e16f3e9249220836c335e97aae4da7b7082566a8483e16f0f667. Captured local VM and final enrollment smoke used these inputs. Do not edit its published tree.

## Enrollment-clock follow-up verified locally

`guest-enrollment.ts` now uses database time for expiry, cooldown, cache reuse, issuance and consumption. Its completion transaction locks allocation/bootstrap rows and rechecks retirement/expiry after signing. No schema changes. `tests/enrollment.test.ts` uses an explicit mocked database-clock port for elapsed retry/expiry unit cases, and real database time with a skewed application clock for the new regressions. Focused34860 passed13tests and typecheck. Full27453 passed372tests/34files, typecheck/lint in87.38s; formatting66781 passed. Native95533 passed actual Smallstep/SSH/TLS enrollment and renewal with cleanup; logs `.local/enrollment-clock-final-check.log`, `.local/enrollment-clock-native.log`. Earlier full32711 stopped at lint because a new test response needed an explicit unknown boundary; corrected before27453. gpt-5.6-sol review found no material blocker. This follow-up is ready to commit; exact CI is pending. See docs/research/m1-enrollment-clock-review.md. The remaining `guest-bootstrap.ts` preparation/render expiry checks still use Date.now and should be aligned as part of customer runtime readiness.

## Previous verified image checkpoint

Branch `yahor/agent-cloud`; private origin https://github.com/yahorbarkouski/agent-cloud.git. Durability correction is committed and pushed as `7bb3497677fb68867f7ba5cf5682a608a1d94814`. Exact [CI34111421868](https://github.com/yahorbarkouski/agent-cloud/actions/runs/34111421868) passed 352 tests in33files, typecheck/lint, formatting, fresh PKI, native smokes and cleanup. Result `.local/snapshot-durability-ci-result.json`. At that checkpoint17 immutable migrations0000–0016 were applied; latest hash569599f36dc2807f8c52acda47b867805a7bb8bcfaeeb09a524afd0146e8e048.

Corrected Hetzner build `de7d3f33-0f3e-4512-afc6-d163b1cd7359` passed installation, sanitation, graceful shutdown, snapshot creation, fresh verifier enrollment/runtime and signed release selection. It was explicitly started at10:32:08Z and fully cleaned at10:35:25.733Z. Local access was removed before signing at10:35:23.762Z. Independent provider inventory at10:37:06.033Z was zero servers, primary IPs, snapshots, firewalls and SSH keys. Post-cleanup validation at10:38:05.889Z confirmed historical signature validity, rejection of current selection, exact snapshot absence, zero open builds and absent local access. Direct cleanup replay passed. Evidence: `docs/research/m1-hetzner-durability-drill.json` and `m1-hetzner-durability-release.json`. The signed release is historical audit evidence; no deployable snapshot remains.

All seven corrected-drill resources are absent: firewall11586440, key118462990, builderIP148449666, builder164935711, snapshot429149547, verifierIP148449870, verifier164936376. Private records use `.local/live-durability-final-*`; independent inventory is `.local/hetzner-durability-final-inventory.json`. Monitor60918 completed exit0. Temporary image API88574, worker36329 and tunnel67714 are stopped. No paid resource or native VM smoke remains active.

The first build `bbb28cd4-aca1-4a94-87cf-364728bf6771` failed enrollment after snapshot. Read-only rescue inspection found zero-byte runtime files after hard poweroff; dirty installation-data loss is a supported inference, not sole-cause block-level proof. It was fully cleaned at09:59:14Z with independent zero inventory. Preserve its separate failure evidence in `docs/research/m1-hetzner-image-drill.json`.

## Durability invariants and local proof

Sanitation stops the random-seed writer before erasing its seed and flushes the root filesystem before every receipt, including replay. Sync errors return no receipt. The historical `power_off` journal command now submits graceful `/actions/shutdown`; only actual observed off authorizes a snapshot. There is no force-off fallback.

Final native57208 passed two distinct clones, injected sync failure/retry, seed lifecycle, restricted SSH, unhealthy-runtime/reboot checks, token scans and complete cleanup. Log `.local/snapshot-durability-native-final.log`. The earlier seed-native73407 failure came from OrbStack reporting lxc and skipping the stock unit. The local-only `scripts/support/vm-seed.ts` fixture clears that condition and requires clone boot to restart the real unit without manual intervention. This is unit-lifecycle proof under an explicit local override; published image inputs and Hetzner installation contain no override. Intermediate failures remain separate in `docs/research/m1-snapshot-durability-verification.json`.

The gpt-5.6-sol reviewer `/root/image_release_judge` found no remaining implementation blocker, then reviewed corrected live evidence with no cleanup/evidence blocker. Review was artifact/trail based; no transcript directory was supplied. Reports are under `docs/research/`. Documentation/evidence after7bb3497 is being checkpointed separately; do not attribute its future commit to the prior exact CI.

## Local processes and secrets

Use `npm exec --yes --package=pnpm@12.3.4 -- pnpm <command>`. TS scripts importing src/\*.js need tsx: `node --env-file=.env --import tsx scripts/check-hetzner.ts`. Pnpm uses Node24.10.0; system Node26.5. Tests use `pnpm exec vitest run <paths>`.

PostgreSQL17 at127.0.0.1:55439, db/useragentcloud, passwordlocal-development-only. Smallstep CA https://localhost:9449,256MiB/.5CPU, pinned step0.30.6/step-ca0.30.2. Current persistent simulated API15970 and worker63771 serve4319. `.env` stays simulated. `.local/admin.credentials.json`, `.local/hcloud-token` and runtime/PKI secrets are0600. Never print secrets or put them in argv/guest data. Hetzner projectagent-cloud-development15945891; Default untouched.

Runtime `.local/runtime.json` is image_factory only, pointing to `.local/runtime-identity`, `.local/guest-builds`, `.local/image-access` and local PKI. Preserve original private material. Public release key ID6c4eb920bf75d1b753253c327082b3c1386136928830e50779b9efc2e1c1a481. Policy reloads for every authorization; restart API and worker after private material/CA changes. The stopped quick tunnel URL must not be reused; a fresh tunnel will have a new URL.

Previous durability input manifest61b1e3972798d86ebba0d8d104912aa9a9158d98e1f91ef76bb3d3ac8ec62329, versiondev-d71c36cede486069faa8e373, input digestd71c36cede486069faa8e373ba98dff20a11677d63660066b8db620bd601cc8d, transferchecksum8632f4ea2d009b3a3cec885d607c7d6493567935ac79cdd1c65cff7a0f0476de. Its `.local/guest-builds/<manifest>` tree is immutable. Earlier unadmitted d2522957 preparation was abandoned and its keys removed.

## Next work

Commit the enrollment clock follow-up and verify its exact CI, then wire the implemented renewal service and required release selection into customer runtime. Complete remaining operator recovery before activation. Customer mode must require signed release selection and preserve admission pins. Reuse the existing enrollment/readiness and operation journals. Continue M2–M7 access/device login, persistent commands/file transfer, Compose deployment, routes/domains, recipes/backups/restore and self-hosting. Image success does not complete M1 or application deployment.

For a later paid test, read current inventory/prices, use reachable HTTPS and explicit CPX12 nbg1 caps. The completed drill admitted at most90minutes, USD120000 VM/IP gross micros and1000000 monthly snapshot micros,40GB, one open build; these are caps, not actual invoice. Keep ownership and cleanup available throughout. Recovery: `pnpm image:build cleanup <build-id>` requires no runtime identity/PKI/source/pricing. Never replace an uncertain create.
