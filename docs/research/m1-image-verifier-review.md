# M1 image verifier review

Reviewed by `gpt-5.6-sol` against implementation commit `5445f410f34d73aa8810a9b77b230defec28ff84`. This was a read-only code, artifact and decision-trail review. No active-workspace transcript directory was supplied, so this review makes no transcript-completeness claim.

## Result

No remaining implementation blocker was found in the reviewed platform-verifier scope. The implementation gives the verifier its own build-owned subject and records, binds its encrypted bootstrap and provider effects to the admitted snapshot, requires current effect-labelled provider ownership before pinned first SSH and runtime reads, persists bounded signing attempts, and rechecks active build state before committing issued identity or verified runtime evidence. Customer allocation and verifier namespaces remain distinct. Full abort includes verifier resources and preserves the existing resource-journal cleanup rules.

The final local evidence supports these claims for the local and simulated boundaries: full session `96323` passed 306 tests across 28 files plus typecheck and lint; format session `60055` passed; native session `45162` exercised two customer clones and a build-owned verifier, rejected unhealthy services, saved a healthy distinct-machine runtime, and completed full abort; cleanup check `517b33` recorded no Orb machines, ownership records, or private builder/verifier fixtures. Migration session `56277a` applied migration 0014, and hash check `d2a9ff` reports all 15 migration hashes matching. Local CLI session `21371` passed and base-state check `abf872` reports zero active allocations, simulated servers, simulated primary IPs, and open image builds. GitHub Actions run `34094753361` completed successfully for the exact implementation commit at `2026-09-07T07:20:45Z`; its check job included the frozen install, full check, format check, PKI, SSH and enrollment smoke steps.

## Findings and resolution

- **Resolved: cancellation and expiry races.** The first review requested a cancellation-during-runtime-read regression. The focused verifier suite now covers cancellation before the result commit, and enrollment tests cover expiry during signing. The controller and SQL completion paths both require an active, unexpired build before durable progress.
- **Resolved: SQL evidence bounds.** Migration 0014 reconstructs the accepted JSON shapes and now rejects malformed UUIDs, oversized identity fields, and fractional or unsafe-size disk evidence. Identity transitions remain immutable, signing attempts are append-only and phase-limited, and verifier results are insert-only and tied to the issued identity, sanitized builder, admitted manifest, snapshot, and distinct machine identity.
- **Resolved: unseeded SSH exposure.** Native session `16223` found that Ubuntu could re-enable the SSH socket before NoCloud seed installation. Sanitation now installs `ConditionPathExists` drop-ins on both `ssh.service` and `ssh.socket`, reloads systemd, and only then removes builder access and records sanitation. Manual check `dc00d5` and the rebuilt native run `45162` observed no guest identity, principals, or listeners on ports 22, 8081, and 8443 before seeding. Enrollment still explicitly starts SSH after publishing the clone's new host key and restricted principals.
- **Resolved: local fixture limitation is explicit.** OrbStack starts the verifier clone before the fixture can install NoCloud data. The unseeded isolation assertion addresses the local exposure risk, while the architecture, AGENTS instructions, and verification artifact correctly avoid treating the seeded reboot as proof of provider initial-user-data injection.

## Trail resolution

All four trail findings from the first final pass are resolved. The verification artifact now records format session `60055`, marks this independent review completed, and records exact-head GitHub Actions run `34094753361` as successful. Independent `gh run view` inspection confirmed status `completed`, conclusion `success`, head SHA `5445f410f34d73aa8810a9b77b230defec28ff84`, and check-job completion at `2026-09-07T07:20:45Z`. The append-only decision log retains the earlier `16223` failure and adds a final `2026-09-07T07:23:22Z` row recording native session `45162`, cleanup `517b33`, and the successful Linux run. No stale pending checkpoint claim remains in the reviewed artifacts.

## Remaining limitations

The native run proves real local Ubuntu guest behavior over actual SSH, HTTPS enrollment, systemd, PKI, runtime inspection, and cleanup. Provider resources and snapshot observations in that run remain protocol fixtures. It does not prove Hetzner initial-boot user-data delivery, a live provider snapshot's ownership or boot source, retained snapshot cleanup, signed release promotion and consumption, certificate renewal or operator recovery, production worker/CLI activation, or completion of M1 and the wider product milestones. No paid provider resource was created during this checkpoint.
