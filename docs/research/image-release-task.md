# Operator image publication design task

Produce one bounded design package for extending this TypeScript repository from locally sanitized images to automated, recoverable Hetzner image publication. Read `image-release-grounding.md`, AGENTS.md and the relevant code. Follow the architect runner prompt and rationale template under `/Users/yahorbarkouski/.codex/skills/architect/references/`. You are a candidate in an already active arena; do not spawn agents or implement code. Keep your package to about1500words plus essential type/signature sketches, and write only your assigned file.

The caller needs a concrete operator command to admit/build, inspect/resume and clean one image build. It must stage the full public input set, provision a disposable builder with temporary management access, install and sanitize it, stop it, create/reconcile a snapshot, verify a fresh boot, and produce a release the existing GuestImage/bootstrap path can consume. A verification run cleans everything. A retained release must have explicit retention and cleanup policy. Do not invent a customer allocation or expand this into an arbitrary workflow framework.

Address builder authentication, not just snapshot HTTP calls. Builder management must use a dedicated key and pinned host identity, without inheriting SSH config/agent or putting fleet/CA/provider credentials into guests. Temporary SSH keys, firewall rules and Primary IPs are owned resources too. Current local CA and development PostgreSQL are available. The live enrollment endpoint may use an explicit temporary HTTPS tunnel; guest identity still comes from direct SSH proof to the provider-observed IP.

The manifest currently binds only bundle/components/trust. Define full installer/service/tool input provenance without self-referential hashes, and link admitted inputs, sanitation receipt, provider image and released GuestImage. Include signed release metadata and its trust/key lifecycle if needed for consumption, rather than implying a checksum alone authenticates a release. No production guest exists, so do not add compatibility machinery for unshipped manifest formats.

Read-only live evidence in `hetzner-image-catalog-check-2026-09-07.json` shows USD/23%VAT, CPX12 nbg1 available with1vCPU/2GiB/40GB and gross27798microUSD/hour including IPv4. Snapshot price is gross24477microUSD perGB-month. Refresh before mutation. All six relevant resource counts are zero. No warm pool, expensive fallback or provider benchmark. Define a bounded test spending envelope and deadline, including a conservative storage-size bound; powered-off builders still cost money.

Every provider effect needs persisted intent before I/O and one-way outcome recording. Lost responses must not blindly create twice. Reconcile exact stable ownership labels and resource identity; label values max63characters. Track duplicate/uncertain resources explicitly rather than silently choosing or deleting them. Handle interruption after installation, sanitation/access loss, VM stop, snapshot creation, source deletion and snapshot deletion. Cleanup must work after deadline or failure without admitting new paid capacity. The existing allocation journal's invariants are valuable, but its customer FKs/budgets do not fit a platform builder.

Write caller usage first, then data structures/signatures/module map, alternatives and tradeoffs. Name exact modules to change and meaningful tests, including native and low-cost provider proof. Keep existing enrollment/PKI/runtime correctness and production startup gates intact until the complete wiring is verified. The full objective still includes customer access, deployments, routes, backups/restore and self-hosting; this package owns the image publication boundary only.
