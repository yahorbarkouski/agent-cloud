# Guest identity design task

Produce a bounded implementation sketch for M1 guest bootstrap/enrollment/readiness, integrated with the existing resource lifecycle. Read `guest-grounding.md` and actual referenced code, AGENTS.md, original-plan sections 9.3 and 10.1, and `/Users/yahorbarkouski/.codex/skills/architect/references/runner-prompt.md` plus its rationale template. This is the architect/arena design phase; do not spawn more agents.

The caller runs the existing CLI create/wait and must get a verified Linux guest with Docker/Compose and a guest HTTP proxy, suitable for later M2 SSH access and M3 deployments. The provider controller already journals explicit IP then VM. Local-first checks and one bounded cheap real Hetzner test are desired, with no warm pool, Stripe or expensive VMs. The account is USD and CPX12 was last USD .027798/hour including IPv4; do not mutate cloud resources or access credentials for this design.

Use ordinary OpenSSH and established certificate tooling, never implement SSH cryptography. Preserve the planned Smallstep/OpenSSH CA model and unique guest-generated SSH/TLS keys. The control plane must establish provider ownership/network identity plus a short-lived bootstrap secret before signing; enrollment retries may accept only the same keys/allocation. Do not distribute provider/CA keys to guests or log cloud-init credentials. Readiness must not be inferred from provider running state. Prepared/unknown provider attempts remain unreplayed, cleanup keeps the existing safety rules. Deal concretely with secret generation/storage across journal crashes, admission retries, and asynchronous guest boot before provider action reconciliation.

Output caller usage first, then types/signatures with stub bodies or pseudocode, module/schema changes and a one-page rationale. Make internal M1 verification a real vertical path without claiming M2 tickets/revocation already exist. Give a concrete low-cost self-hosted development verification path: local API is currently only on loopback; address how a real guest can reach enrollment without assuming a pre-existing domain or accepting arbitrary proxy headers. The final target is a prebuilt sanitized image; distinguish temporary image-build boot from the shipped first-boot path. Prefer few explicit modules and existing durable records. State staged implementation so the next action is executable.

Write only your assigned candidate markdown, no implementation or cloud mutation. Maximum 1500 words including code. Cite official sources for any niche/version-sensitive facts you rely on; browsing is allowed, secrets are not needed.
